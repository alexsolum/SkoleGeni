---
phase: 04-durable-class-editing
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - .planning/REQUIREMENTS.md
  - src/pages/__tests__/class-editor-sidebar.test.tsx
autonomous: true
requirements: [EDIT-02]
gap_closure: true

must_haves:
  truths:
    - "The written EDIT-02 contract matches the locked Phase 4 warn-only editing decision."
    - "Executable verification proves invalid manual states stay editable while the rule explanation remains visible."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Canonical Phase 4 requirement wording for manual-edit conflict handling"
      contains: "EDIT-02"
    - path: "src/pages/__tests__/class-editor-sidebar.test.tsx"
      provides: "Regression coverage for red-card and sidebar explanation behavior"
      contains: "Ada cannot be in the same class as Bea."
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "src/pages/ClassEditor.tsx"
      via: "requirement wording aligned to implemented warn-only conflict UX"
      pattern: "EDIT-02"
    - from: "src/pages/__tests__/class-editor-sidebar.test.tsx"
      to: "src/pages/ClassEditor.tsx"
      via: "rendered conflict count, red-card state, and issue copy"
      pattern: "Conflicts: 2|data-red-card"
---

<objective>
Close the Phase 4 contract mismatch around invalid manual moves without reopening the editor architecture.

Purpose: Honor the locked "warn only" decision from `04-CONTEXT.md` and make that decision executable through regression coverage.
Output: Updated `EDIT-02` wording plus a test that proves the red-card/sidebar explanation path remains the intended behavior.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-durable-class-editing/04-CONTEXT.md
@.planning/phases/04-durable-class-editing/04-RESEARCH.md
@.planning/phases/04-durable-class-editing/04-VERIFICATION.md
@.planning/phases/04-durable-class-editing/04-03-SUMMARY.md
@src/pages/ClassEditor.tsx
@src/pages/__tests__/class-editor-sidebar.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align EDIT-02 with the locked warn-only manual-edit contract</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/04-durable-class-editing/04-CONTEXT.md, .planning/phases/04-durable-class-editing/04-RESEARCH.md, .planning/phases/04-durable-class-editing/04-VERIFICATION.md, src/pages/ClassEditor.tsx</read_first>
  <action>Update `EDIT-02` in `.planning/REQUIREMENTS.md` so it no longer says invalid manual moves are blocked. Replace the manual-editing requirement sentence with this exact contract: `User can make an invalid manual move, immediately sees it flagged as a conflict, and receives a clear explanation of the violated rule while continuing to edit.` Keep the requirement ID `EDIT-02`, keep its Phase 4 mapping, and do not add any wording that reintroduces hard blocking or deferred branching/collaboration ideas.</action>
  <acceptance_criteria>`rg -n "EDIT-02" .planning/REQUIREMENTS.md` returns the updated requirement, and the line contains the exact phrases `invalid manual move`, `flagged as a conflict`, and `continuing to edit`.</acceptance_criteria>
  <verify>
    <automated>rg -n "EDIT-02.*invalid manual move.*flagged as a conflict.*continuing to edit" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>The canonical requirement text now matches the Phase 4 user decision to allow invalid intermediate states with immediate explanation.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the warn-only behavior into sidebar regression coverage</name>
  <files>src/pages/__tests__/class-editor-sidebar.test.tsx</files>
  <read_first>src/pages/__tests__/class-editor-sidebar.test.tsx, src/pages/ClassEditor.tsx, src/lib/rosterValidation.ts, .planning/phases/04-durable-class-editing/04-03-SUMMARY.md</read_first>
  <action>Strengthen `src/pages/__tests__/class-editor-sidebar.test.tsx` so it verifies the current contract instead of only incidental copy. Keep the seeded invalid roster with `p1` and `p2` together, then assert all of these exact states after render: the header shows `Conflicts: 2`, at least one rendered pupil card has `data-red-card=\"true\"`, the Manual Issues sidebar includes `Negative chemistry`, and the explanation text `Ada cannot be in the same class as Bea.` is visible. Retain the existing hover-to-highlight assertion so the test proves invalid states remain editable and explainable instead of blocked.</action>
  <acceptance_criteria>The sidebar test contains assertions for `Conflicts: 2`, a `[data-red-card='true']` selector, `Negative chemistry`, and `Ada cannot be in the same class as Bea.`; `npx vitest run src/pages/__tests__/class-editor-sidebar.test.tsx` passes.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/class-editor-sidebar.test.tsx</automated>
  </verify>
  <done>Regression coverage now proves the intended EDIT-02 behavior: invalid moves surface red-card/sidebar feedback while the editor remains in a usable state.</done>
</task>

</tasks>

<verification>
Run `rg -n "EDIT-02" .planning/REQUIREMENTS.md` and `npx vitest run src/pages/__tests__/class-editor-sidebar.test.tsx`.
</verification>

<success_criteria>
`EDIT-02` no longer contradicts the locked Phase 4 decision, and the warn-only conflict UX is covered by a passing regression test.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-class-editing/04-04-SUMMARY.md`
</output>
