# Deferred Items

- `tests/e2e/durable-editing.spec.ts` is referenced by `04-02-PLAN.md` verification, but the Playwright spec does not exist in the repository yet. Browser-level durable editing verification remains deferred.
