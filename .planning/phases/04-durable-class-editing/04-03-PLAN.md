---
phase: 04-durable-class-editing
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [src/lib/rosterValidation.ts, src/lib/rosterValidation.test.ts, src/pages/ClassEditor.tsx]
autonomous: true
requirements: [EDIT-02]

must_haves:
  truths:
    - "Hard rule violations (negative chemistry, size limits) are visually marked with a 'Red Card' state"
    - "A persistent sidebar lists all current manual violations in the roster"
    - "Soft-rule score drops are surfaced inline while the user edits"
    - "Manual moves do not block the UI but provide immediate, clear violation feedback"
  artifacts:
    - path: "src/lib/rosterValidation.ts"
      provides: "Exhaustive rule validation engine matching Python solver"
    - path: "src/pages/ClassEditor.tsx"
      provides: "Red Card UI, Issues Sidebar, and inline score-drop warnings"
  key_links:
    - from: "src/pages/ClassEditor.tsx"
      to: "src/lib/rosterValidation.ts"
      via: "validation call on state change"
---

<objective>
Build trust in the manual editing process by providing high-fidelity rule feedback that matches the optimizer's scoring model.

Purpose: Ensure users understand the tradeoffs and violations created by their manual moves.
Output: Validation engine, "Red Card" card states, issues sidebar, and inline score-drop warnings.
</objective>

<execution_context>
@C:/Users/HP/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-durable-class-editing/04-01-SUMMARY.md
@.planning/phases/04-durable-class-editing/04-02-SUMMARY.md
@.planning/phases/04-durable-class-editing/04-VALIDATION.md
@src/pages/ClassEditor.tsx
@api/optimizer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the JS Roster Validation Engine</name>
  <files>src/lib/rosterValidation.ts, src/lib/rosterValidation.test.ts</files>
  <behavior>
    - Test 1: Identify negative chemistry links within the same class.
    - Test 2: Identify class size violations (min/max size).
    - Test 3: Calculate detailed scores (gender, origin, needs, location, chemistry).
    - Test 4: Parity check with Python: ensure integer-based math stays within 1% tolerance.
  </behavior>
  <action>
    Implement `src/lib/rosterValidation.ts` to mirror Python's scoring logic exactly.
    1. Create `validateRoster(assignment, pupils, chemistry, constraints)` returning `violations` and `scores`.
    2. Ensure TS scoring logic matches Python's CP-SAT rules (use integer-based math or precise rounding).
    3. Include both "Hard" violations (Red Card) and "Soft" score drops.
  </action>
  <verify>
    <automated>vitest src/lib/rosterValidation.test.ts</automated>
  </verify>
  <done>Exhaustive validation engine is implemented and verified with tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement "Red Card" State and Tooltips</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Update `ClassEditor.tsx` UI to use `validateRoster`.
    1. Update `DraggablePupilCard` to show a "Red Card" state (e.g., `shadow-red-500` and red border) if it has a 'hard' violation.
    2. Add a tooltip/hover effect to the card that explains the violation reason.
    3. Ensure manual moves allow "broken" states (no hard-blocks).
    4. Implement "Warn on Score Drop": Visually flag categories where manual edits significantly drop the score compared to the optimizer's result.
  </action>
  <verify>
    <automated>vitest src/components/PupilCard.test.tsx</automated>
  </verify>
  <done>User receives immediate, visual feedback on rule violations and score drops during manual moves.</done>
</task>

<task type="auto">
  <name>Task 3: Create Manual Issues Sidebar</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Add a persistent sidebar to the Class Editor.
    1. List all active violations: Pupil name, Rule name, and Detailed reason.
    2. Add a "Conflicts: [N]" badge to the editor header.
    3. Implement "Hover to highlight": hovering over an issue in the sidebar highlights the corresponding pupil card.
  </action>
  <verify>
    <automated>vitest src/pages/__tests__/class-editor-sidebar.test.tsx</automated>
  </verify>
  <done>User can see all roster violations in a centralized list for easier resolution.</done>
</task>

</tasks>

<verification>
Manual verification: Move two pupils with negative chemistry into the same class. Verify red cards, score-drop warnings, and sidebar updates without the move being blocked.
Run: `npx playwright test tests/e2e/durable-editing.spec.ts` (EDIT-02 coverage).
</verification>

<success_criteria>
1. Hard violations (chemistry, size) are visually flagged (Red Card).
2. Centralized issues sidebar lists all current roster violations.
3. Soft-rule score drops are surfaced inline while manual editing stays warn-only.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-class-editing/04-03-SUMMARY.md`
</output>
