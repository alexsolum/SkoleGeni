---
phase: 04-durable-class-editing
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [src/lib/rosterValidation.ts, src/lib/rosterValidation.test.ts, src/pages/ClassEditor.tsx]
autonomous: true
requirements: [EDIT-02]

must_haves:
  truths:
    - "Hard rule violations (e.g., negative chemistry) are visually marked with a 'Red Card' state"
    - "A persistent sidebar lists all current manual violations in the roster"
    - "Manual moves do not block the UI but provide immediate, clear violation feedback"
  artifacts:
    - path: "src/lib/rosterValidation.ts"
      provides: "Exhaustive rule validation engine matching Python solver"
    - path: "src/pages/ClassEditor.tsx"
      provides: "Red Card UI and Issues Sidebar"
  key_links:
    - from: "src/pages/ClassEditor.tsx"
      to: "src/lib/rosterValidation.ts"
      via: "validation call on state change"
---

<objective>
Build trust in the manual editing process by providing high-fidelity feedback and scoring that matches the optimizer's rules.

Purpose: Ensure users understand the tradeoffs and violations created by their manual moves.
Output: Validation engine, "Red Card" card states, and a persistent issues sidebar.
</objective>

<execution_context>
@C:/Users/HP/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-durable-class-editing/04-02-SUMMARY.md
@src/pages/ClassEditor.tsx
@api/optimizer.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the JS Roster Validation Engine</name>
  <files>src/lib/rosterValidation.ts, src/lib/rosterValidation.test.ts</files>
  <behavior>
    - Test 1: Identify negative chemistry links within the same class.
    - Test 2: Identify class size violations (min/max size).
    - Test 3: Calculate detailed scores for gender mix, origin school balance, and needs distribution.
  </behavior>
  <action>
    Implement `src/lib/rosterValidation.ts` to mirror Python's scoring logic.
    1. Create a function `validateRoster(assignment, pupils, chemistry, constraints)` that returns:
       - `violations: { pupilId: string, rule: string, severity: 'hard' | 'soft', reason: string }[]`
       - `scores: Score` (full detailed scores).
    2. Use the "Shadow Scoring" pattern: ensure TS scoring logic matches Python's CP-SAT rules for consistency.
    3. Ensure JS uses integer-based math or precise rounding to stay within 1% tolerance of the solver's score.
  </action>
  <verify>
    <automated>npm run test src/lib/rosterValidation.test.ts</automated>
  </verify>
  <done>Exhaustive validation engine is implemented and verified with tests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement "Red Card" State and Tooltips</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Update the `ClassEditor` UI to reflect the validation results.
    1. Integrate `validateRoster` into the component (call it on assignment change).
    2. Update `DraggablePupilCard` to show a "Red Card" state (red shadow or border) if it has a 'hard' violation.
    3. Use a tooltip or popover (or `title` attribute for simplicity) on the card to explain the violation.
    4. Ensure "Soft" rule warnings (significant score drops) are reflected on the relevant class summary or category scores.
    5. Remove the "hard-block" on drag-and-drop moves to allow "broken" states as per CONTEXT.md.
  </action>
  <verify>
    <automated>grep "Red Card" src/pages/ClassEditor.tsx || grep "shadow-red" src/pages/ClassEditor.tsx</automated>
  </verify>
  <done>User receives immediate, visual feedback on rule violations during manual moves.</done>
</task>

<task type="auto">
  <name>Task 3: Create Manual Issues Sidebar</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Add a persistent sidebar (or a toggleable panel) to the Class Editor.
    1. Create an `IssuesSidebar` that lists all currently active violations.
    2. For each violation, show:
       - Pupil name.
       - Rule name (e.g., "Negative Chemistry").
       - Detailed reason (e.g., "Must not be with [Other Pupil]").
    3. Add a "Conflicts: [N]" badge to the editor header to indicate the count of violations.
    4. Implement "Hover to highlight": hovering over an issue in the sidebar should highlight the corresponding pupil card in the editor.
  </action>
  <verify>
    <automated>grep "IssuesSidebar" src/pages/ClassEditor.tsx</automated>
  </verify>
  <done>User can see all roster violations in a centralized list for easier resolution.</done>
</task>

</tasks>

<verification>
Manual verification: Move two pupils with negative chemistry into the same class. Verify both cards turn red and an issue appears in the sidebar.
</verification>

<success_criteria>
1. Negative chemistry and class size violations are visually flagged.
2. Sidebar correctly lists all violations in the current manual roster.
3. Scoring meters match the Results page in detail and accuracy.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-class-editing/04-03-SUMMARY.md`
</output>
