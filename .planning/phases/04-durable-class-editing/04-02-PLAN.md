---
phase: 04-durable-class-editing
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [src/pages/ClassEditor.tsx, src/lib/editorStore.ts]
autonomous: true
requirements: [EDIT-01, EDIT-03]

must_haves:
  truths:
    - "Manual moves are automatically saved to the database with a short debounce"
    - "User can undo and redo manual moves using interface buttons"
    - "Manual edits are cleared if the underlying pupil data is updated"
  artifacts:
    - path: "src/pages/ClassEditor.tsx"
      provides: "Undo/Redo UI and Autosave hook"
  key_links:
    - from: "src/pages/ClassEditor.tsx"
      to: "supabase.roster_assignments"
      via: "debounced upsert"
---

<objective>
Enable an interactive and reliable editing experience by implementing Undo/Redo support and debounced autosave functionality.

Purpose: Build trust in the editor by ensuring moves are easy to revert and always saved.
Output: Undo/Redo UI controls, debounced background synchronization with Supabase, and draft integrity checks.
</objective>

<execution_context>
@C:/Users/HP/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-durable-class-editing/04-01-SUMMARY.md
@src/lib/editorStore.ts
@src/pages/ClassEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Undo/Redo UI and Keyboard Support</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Integrate `zundo` (temporal) state into the ClassEditor header.
    1. Access the `temporal` API via `useEditorStore.useTemporal.getState()`.
    2. Add "Undo" and "Redo" buttons in the top right header.
    3. Implement keyboard shortcuts: Cmd/Ctrl + Z (Undo) and Cmd/Ctrl + Shift + Z (Redo).
    4. Disable buttons when there is no history (use `undo_stack.length` or similar).
  </action>
  <verify>
    <automated>grep "undo" src/pages/ClassEditor.tsx && grep "redo" src/pages/ClassEditor.tsx</automated>
  </verify>
  <done>Undo/Redo functionality is visible and accessible in the Class Editor.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Debounced Database Autosave</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Create a `useAutosave` hook or effect within `ClassEditor.tsx` (or refactor to a separate file if needed).
    1. Watch for changes in the `assignment` state from `useEditorStore`.
    2. Use `lodash.debounce` to wait 2 seconds after the last change.
    3. Perform an `upsert` to the `roster_assignments` table with:
       - `project_id`, `assignment`, `score_json` (current `score` state).
    4. Handle loading/saving states (show a "Saving..." or "All changes saved" indicator in the UI).
  </action>
  <verify>
    <automated>grep "debounce" src/pages/ClassEditor.tsx</automated>
  </verify>
  <done>Manual edits are automatically synced to the database in the background.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Draft Integrity and "Clear on Pupil Change"</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Add a check during hydration to ensure manual edits are still valid.
    1. Fetch the `updated_at` timestamp from the latest `pupils` update for this project.
    2. Fetch the `updated_at` timestamp from `roster_assignments`.
    3. If `pupils` timestamp is newer than `roster_assignments` timestamp:
       - Clear the `roster_assignments` record (or prompt the user).
       - Fallback to the latest `optimization_runs`.
       - Show a notification: "Manual edits cleared because pupil data has changed."
  </action>
  <verify>
    <automated>grep "updated_at" src/pages/ClassEditor.tsx</automated>
  </verify>
  <done>Manual assignments stay in sync with the underlying pupil data state.</done>
</task>

</tasks>

<verification>
Manual verification: Move a pupil, wait for "Saved" indicator, then click "Undo". The pupil should return to the original class. Refresh the page to confirm the undo persisted.
</verification>

<success_criteria>
1. User can undo/redo manual moves via UI buttons.
2. Background saves happen automatically (no Save button needed).
3. "All changes saved" indicator reflects the actual DB sync status.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-class-editing/04-02-SUMMARY.md`
</output>
