---
phase: 04-durable-class-editing
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/__tests__/class-editor-hydration.test.tsx
  - src/pages/__tests__/class-editor-integrity.test.tsx
  - src/pages/ClassEditor.tsx
  - tests/fixtures/durableEditingData.ts
  - tests/helpers/durableEditingSupabaseRoutes.ts
  - tests/durable-editing.spec.ts
autonomous: true
requirements: [EDIT-01, EDIT-03]
gap_closure: true

must_haves:
  truths:
    - "Persistence and recovery flows are covered by executable browser verification."
    - "Hydration and integrity integration tests exercise the current Supabase query chain instead of stale mocks."
    - "Saved manual assignments and reset-to-optimizer recovery remain provable after future edits."
  artifacts:
    - path: "tests/durable-editing.spec.ts"
      provides: "Browser-level persistence and recovery verification for the class editor"
      contains: "durable editing persistence and recovery"
    - path: "tests/helpers/durableEditingSupabaseRoutes.ts"
      provides: "Deterministic route-stubbed auth and Supabase seams for editor testing"
      contains: "installDurableEditingSupabaseRoutes"
    - path: "src/pages/__tests__/class-editor-hydration.test.tsx"
      provides: "Hydration priority coverage using the pupils created_at query chain"
      contains: "created_at"
    - path: "src/pages/__tests__/class-editor-integrity.test.tsx"
      provides: "Recovery coverage with unambiguous DOM assertions"
      contains: "EDITOR_STORAGE_NAME"
  key_links:
    - from: "tests/durable-editing.spec.ts"
      to: "tests/helpers/durableEditingSupabaseRoutes.ts"
      via: "Playwright route stubs and seeded auth/session state"
      pattern: "installDurableEditingSupabaseRoutes"
    - from: "src/pages/__tests__/class-editor-hydration.test.tsx"
      to: "src/pages/ClassEditor.tsx"
      via: "matching pupils created_at query chain"
      pattern: "order\\(\"created_at\""
    - from: "src/pages/__tests__/class-editor-integrity.test.tsx"
      to: "src/pages/ClassEditor.tsx"
      via: "specific card or storage assertions without ambiguous text queries"
      pattern: "getAllByText|querySelector"
---

<objective>
Restore the missing Phase 4 verification surface for durable editing persistence and recovery.

Purpose: Close the browser-test hole and repair the two drifted Vitest files so Phase 4 durability claims are executable again.
Output: Passing hydration/integrity tests plus a deterministic Playwright spec for move persistence, reload recovery, and reset recovery.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-durable-class-editing/04-VERIFICATION.md
@.planning/phases/04-durable-class-editing/04-01-SUMMARY.md
@.planning/phases/04-durable-class-editing/04-02-SUMMARY.md
@src/pages/ClassEditor.tsx
@src/pages/__tests__/class-editor-hydration.test.tsx
@src/pages/__tests__/class-editor-integrity.test.tsx
@tests/phase2-workflow.spec.ts
@tests/helpers/phase2SupabaseRoutes.ts
@tests/fixtures/phase2WorkflowData.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair the drifted hydration and integrity integration tests</name>
  <files>src/pages/__tests__/class-editor-hydration.test.tsx, src/pages/__tests__/class-editor-integrity.test.tsx</files>
  <read_first>src/pages/__tests__/class-editor-hydration.test.tsx, src/pages/__tests__/class-editor-integrity.test.tsx, src/pages/ClassEditor.tsx, src/lib/editorStore.ts, .planning/phases/04-durable-class-editing/04-VERIFICATION.md</read_first>
  <action>Update the two existing Vitest files to match the current editor implementation exactly. In `src/pages/__tests__/class-editor-hydration.test.tsx`, make the mocked `supabase.from(\"pupils\").select(\"created_at\")` path return the current `eq(...).order(\"created_at\", { ascending: false }).limit(1).maybeSingle()` chain used by `ClassEditor.tsx`. In `src/pages/__tests__/class-editor-integrity.test.tsx`, remove the ambiguous `getByText(\"Ada\")` style assertion and replace it with an unambiguous check against the restored assignment or a specific rendered element, such as the persisted `EDITOR_STORAGE_NAME` payload or a targeted selector that can only match the card list once. Do not change production behavior in this task.</action>
  <acceptance_criteria>`npx vitest run src/pages/__tests__/class-editor-hydration.test.tsx src/pages/__tests__/class-editor-integrity.test.tsx` passes, the hydration test mock includes `order(\"created_at\"` and `limit(1)`, and the integrity test no longer uses a single ambiguous `getByText(\"Ada\")` assertion.</acceptance_criteria>
  <verify>
    <automated>npx vitest run src/pages/__tests__/class-editor-hydration.test.tsx src/pages/__tests__/class-editor-integrity.test.tsx</automated>
  </verify>
  <done>The two existing integration tests once again verify Phase 4 hydration and recovery instead of failing on stale mocks or ambiguous selectors.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic Playwright coverage for durable editing persistence and reset recovery</name>
  <files>src/pages/ClassEditor.tsx, tests/fixtures/durableEditingData.ts, tests/helpers/durableEditingSupabaseRoutes.ts, tests/durable-editing.spec.ts</files>
  <read_first>src/pages/ClassEditor.tsx, tests/phase2-workflow.spec.ts, tests/helpers/phase2SupabaseRoutes.ts, tests/fixtures/phase2WorkflowData.ts, src/lib/api.ts, src/lib/editorStore.ts, .planning/phases/04-durable-class-editing/04-VERIFICATION.md</read_first>
  <action>Create a Phase 4 browser harness that follows the existing route-stubbed authenticated Playwright pattern instead of introducing real Supabase dependencies. Add `tests/fixtures/durableEditingData.ts` exporting a fixed project id, seeded auth session reuse, a saved assignment row, one optimizer baseline, a small pupil set, and deterministic score payloads. Add `tests/helpers/durableEditingSupabaseRoutes.ts` exporting `installDurableEditingSupabaseRoutes(page)` that seeds `sb-localhost-auth-token`, stubs `**/auth/v1/user`, `**/auth/v1/token?grant_type=refresh_token`, `**/rest/v1/project_constraints*`, `**/rest/v1/pupils*`, `**/rest/v1/chemistry_links*`, `**/rest/v1/optimization_runs*`, `**/rest/v1/roster_assignments*`, and `**/api/optimizer/project/score`, and keeps in-memory mutable saved-assignment state across reloads. In `src/pages/ClassEditor.tsx`, add stable browser selectors `data-testid=\"class-dropzone-${classIndex}\"` on each droppable class column and `data-testid=\"pupil-card-${pupil.id}\"` on each draggable pupil card without changing layout or validation logic. Then create `tests/durable-editing.spec.ts` with one spec titled exactly `durable editing persistence and recovery` that uses `dragTo` from `pupil-card-p1` into `class-dropzone-1`, waits for `All changes saved`, reloads and verifies the moved pupil remains in the target class, clicks `Reset to Optimizer Result`, waits for `Restored optimizer assignment.`, and verifies the optimizer baseline is back after the durable row is deleted.</action>
  <acceptance_criteria>`npx playwright test tests/durable-editing.spec.ts` passes, `tests/durable-editing.spec.ts` contains the exact title `durable editing persistence and recovery`, `tests/helpers/durableEditingSupabaseRoutes.ts` exports `installDurableEditingSupabaseRoutes`, and `src/pages/ClassEditor.tsx` contains `data-testid=\"class-dropzone-` plus `data-testid=\"pupil-card-`.</acceptance_criteria>
  <verify>
    <automated>npx playwright test tests/durable-editing.spec.ts</automated>
  </verify>
  <done>Phase 4 durability claims are backed by a reproducible browser spec that proves manual moves persist across reload and reset recovery returns to the optimizer baseline.</done>
</task>

</tasks>

<verification>
Run `npx vitest run src/pages/__tests__/class-editor-hydration.test.tsx src/pages/__tests__/class-editor-integrity.test.tsx` and `npx playwright test tests/durable-editing.spec.ts`.
</verification>

<success_criteria>
The missing browser verification artifact exists and passes, and the hydration plus integrity tests are green against the current editor query and DOM behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-class-editing/04-05-SUMMARY.md`
</output>
