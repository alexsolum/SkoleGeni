---
phase: 04-durable-class-editing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, supabase/migrations/0002_roster_assignments.sql, src/lib/editorStore.ts, src/pages/ClassEditor.tsx]
autonomous: true
requirements: [EDIT-01, EDIT-03]

must_haves:
  truths:
    - "Manual class assignments are stored in a dedicated database table"
    - "Class editor state is managed by a persistent, temporal store (Zustand + Zundo)"
    - "Returning to the Class Editor restores the last saved manual assignment"
  artifacts:
    - path: "supabase/migrations/0002_roster_assignments.sql"
      provides: "Durable persistence for manual edits"
    - path: "src/lib/editorStore.ts"
      provides: "Zustand store with Undo/Redo and local persistence"
  key_links:
    - from: "src/pages/ClassEditor.tsx"
      to: "src/lib/editorStore.ts"
      via: "hook usage"
    - from: "src/lib/editorStore.ts"
      to: "supabase.roster_assignments"
      via: "persistence middleware"
---

<objective>
Establish the data foundation for durable class editing by creating the persistent storage model and a temporal state store.

Purpose: Ensure manual edits are never lost and can be reliably restored across sessions.
Output: Database table for assignments, Zustand store with Undo/Redo support, and a smart hydration mechanism in the editor.
</objective>

<execution_context>
@C:/Users/HP/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-durable-class-editing/04-CONTEXT.md
@.planning/phases/04-durable-class-editing/04-RESEARCH.md
@src/pages/ClassEditor.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup persistence schema and dependencies</name>
  <files>package.json, supabase/migrations/0002_roster_assignments.sql</files>
  <action>
    1. Install `zustand` and `zundo` (2.1.0+) in `package.json`.
    2. Create `supabase/migrations/0002_roster_assignments.sql` with:
       - `roster_assignments` table: `project_id (pk, ref projects)`, `assignment (jsonb, string[][])`, `score_json (jsonb)`, `updated_at`.
       - Enable RLS and add policies: select/insert/update allowed for project owners (use `exists` check on `projects` table like in 0001_init.sql).
       - Add a trigger for `set_updated_at()`.
  </action>
  <verify>
    <automated>npm list zustand zundo && ls supabase/migrations/0002_roster_assignments.sql</automated>
  </verify>
  <done>Dependencies installed and DB schema defined for durable assignments.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Temporal Editor Store</name>
  <files>src/lib/editorStore.ts</files>
  <action>
    Create a Zustand store in `src/lib/editorStore.ts` using the `temporal` (zundo) and `persist` middleware.
    - State: `assignment: string[][]`, `projectId: string | null`, `lastSaved: string | null`.
    - Actions: `setAssignment(next: string[][])`, `initialize(projectId: string, initial: string[][])`, `reset()`.
    - Temporal config: Limit history to 50 steps, partialize to only track `assignment`.
    - Persist config: Name 'roster-draft-storage', use session storage as a secondary buffer.
  </action>
  <verify>
    <automated>test -f src/lib/editorStore.ts</automated>
  </verify>
  <done>Zustand store with Undo/Redo capabilities is ready for integration.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor ClassEditor for Smart Hydration</name>
  <files>src/pages/ClassEditor.tsx</files>
  <action>
    Update `ClassEditor.tsx` to use `useEditorStore` instead of local `useState` for assignment.
    1. Replace local `assignment` state with store state.
    2. Update `useEffect` (loading logic) to:
       - Check `roster_assignments` table for existing manual state.
       - If found, load it.
       - If not found, load from the latest `optimization_runs`.
       - Hydrate the store with the selected state.
    3. Add a "Reset to Optimizer Result" button that discards `roster_assignments` (via Supabase delete) and reloads the original optimizer result.
  </action>
  <verify>
    <automated>grep "useEditorStore" src/pages/ClassEditor.tsx</automated>
  </verify>
  <done>ClassEditor now prioritizes and restores manual edits from durable storage.</done>
</task>

</tasks>

<verification>
Manual verification: Open Class Editor, move a pupil, refresh page. The pupil should stay in the new class.
Check Supabase local dashboard (if available) for records in `roster_assignments`.
</verification>

<success_criteria>
1. `roster_assignments` table is created in the database.
2. `ClassEditor` loads manual assignments if they exist.
3. User can manually reset to the original optimizer result.
</success_criteria>

<output>
After completion, create `.planning/phases/04-durable-class-editing/04-01-SUMMARY.md`
</output>
