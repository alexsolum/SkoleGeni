---
phase: 02-reliable-project-workflow
plan: "03"
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - playwright.config.ts
  - src/pages/__tests__/workflow-reload.test.tsx
  - tests/helpers/phase2SupabaseRoutes.ts
  - tests/fixtures/phase2WorkflowData.ts
  - tests/phase2-workflow.spec.ts
autonomous: false
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - User can create or reopen a project, edit setup and pupils, reload the page, and continue with the saved state intact.
    - Common failure cases leave clear retry states instead of allowing stale progress.
    - The final workflow has both automated regression coverage and a human confirmation pass for the trust-signaling UI.
  artifacts:
    - path: src/pages/__tests__/workflow-reload.test.tsx
      provides: Integration regression coverage for reload and failure-gate behavior
    - path: tests/phase2-workflow.spec.ts
      provides: Browser-level coverage of the full setup-to-save workflow
  key_links:
    - from: tests/phase2-workflow.spec.ts
      to: route flow
      via: welcome -> configuration -> pupils -> reload -> continue
      pattern: /configure/|/pupils/
    - from: src/pages/__tests__/workflow-reload.test.tsx
      to: configuration and pupil pages
      via: mocked Supabase reload and failure states
      pattern: Configuration|PupilData
---

<objective>
Prove the end-to-end project workflow works across reloads and common failure cases, then pause for a focused human trust check on the save-status and CSV-mapping UX.

Purpose: Close Phase 2 by turning the new workflow behavior into repeatable regression checks plus one explicit human verification gate.
Output: Integration and browser-flow regression tests plus a human-approved workflow verification step.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-reliable-project-workflow/02-CONTEXT.md
@.planning/phases/02-reliable-project-workflow/02-VALIDATION.md
@.planning/phases/02-reliable-project-workflow/02-01-PLAN.md
@.planning/phases/02-reliable-project-workflow/02-02-PLAN.md
@src/pages/Configuration.tsx
@src/pages/PupilData.tsx
@src/pages/Welcome.tsx

<interfaces>
Routes from src/App.tsx:
```typescript
<Route path="/" element={<Welcome />} />
<Route path="/configure/:projectId" element={<Configuration />} />
<Route path="/pupils/:projectId" element={<PupilData />} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for reload restore and failure gating behavior</name>
  <files>src/pages/__tests__/workflow-reload.test.tsx</files>
  <read_first>
- src/pages/Welcome.tsx
- src/pages/Configuration.tsx
- src/pages/PupilData.tsx
- src/pages/__tests__/configuration-load.test.tsx
- src/pages/__tests__/pupil-autosave.test.tsx
- .planning/phases/02-reliable-project-workflow/02-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: Reopening a saved project restores the last persisted constraint values and pupil rows instead of defaults.
    - Test 2: Configuration load failure keeps the user on the blocking retry state.
    - Test 3: Autosave failure on pupil edits keeps `Run Optimizer` disabled until retry succeeds.
  </behavior>
  <action>Create `src/pages/__tests__/workflow-reload.test.tsx` as a focused integration suite using mocked Supabase responses and router navigation. Cover the create/reopen/continue path by rendering `Welcome`, navigating to `Configuration`, then `PupilData`, and confirming saved data reappears after remount/reload. Include one failure-path test for configuration load that asserts the exact retry label `Retry loading saved setup`, and one failure-path test for pupil autosave that asserts the exact labels `Save failed` and `Retry save` while `Run Optimizer` stays disabled. Reuse the existing test setup from `src/test/setup.ts`; do not add a second harness.</action>
  <verify>
    <automated>npm run test -- src/pages/__tests__/workflow-reload.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/pages/__tests__/workflow-reload.test.tsx` exists.
- The test file contains the exact labels `Retry loading saved setup`, `Save failed`, and `Retry save`.
- The verify command passes.
  </acceptance_criteria>
  <done>Reload and failure-gate behavior is covered by a dedicated regression suite instead of relying only on manual checking.</done>
</task>

<task type="auto">
  <name>Task 2: Add a browser-level Phase 2 workflow smoke test</name>
  <files>playwright.config.ts, tests/helpers/phase2SupabaseRoutes.ts, tests/fixtures/phase2WorkflowData.ts, tests/phase2-workflow.spec.ts</files>
  <read_first>
- playwright.config.ts
- src/pages/Welcome.tsx
- src/pages/Configuration.tsx
- src/pages/PupilData.tsx
- src/lib/supabase.ts
- .planning/phases/02-reliable-project-workflow/02-VALIDATION.md
  </read_first>
  <action>Make the Playwright smoke test fully runnable and deterministic. In `playwright.config.ts`, rely on the Wave 1 `webServer` contract with the exact command `npm run dev -- --host 127.0.0.1 --port 4173`, URL `http://127.0.0.1:4173`, and `reuseExistingServer: true`. Create `tests/fixtures/phase2WorkflowData.ts` exporting a fixed fake session object, a fixed project id `11111111-1111-4111-8111-111111111111`, one saved constraint row, two pupil rows, and one failing-save toggle payload so the browser test never depends on random ids. Create `tests/helpers/phase2SupabaseRoutes.ts` exporting `installPhase2SupabaseRoutes(page)` that stubs these exact Supabase endpoints with Playwright `page.route`: `**/auth/v1/user`, `**/auth/v1/token?grant_type=refresh_token`, `**/rest/v1/projects*`, `**/rest/v1/project_constraints*`, `**/rest/v1/pupils*`, `**/rest/v1/chemistry_links*`, `**/rest/v1/optimization_runs*`, and `**/rest/v1/rpc/replace_project_roster_state`. The auth strategy must be route-stubbed authenticated mode: before visiting `/`, inject the fixture session into `localStorage` under the exact key `sb-localhost-auth-token` and have `**/auth/v1/user` return the same user id/email as the fixture so `supabase.auth.getSession()` and `onAuthStateChange()` both resolve as signed in without email flows. `installPhase2SupabaseRoutes(page)` must keep an in-memory mutable store for projects, constraints, pupils, chemistry rows, and a single `failNextRosterSave` boolean that makes the first `replace_project_roster_state` call return HTTP 500 with `{ "message": "simulated save failure" }`, then succeed on retry. Create `tests/phase2-workflow.spec.ts` with a single Playwright spec titled exactly `phase-2 workflow` that calls `installPhase2SupabaseRoutes(page)`, seeds the localStorage auth token, creates a project from the Welcome screen, saves valid configuration, enters at least two pupils manually, waits for `All changes saved`, reloads the page, confirms the same pupils still render, flips the failing-save toggle once through the stub helper, verifies `Save failed` plus `Retry save`, clicks retry, and verifies the banner returns to `All changes saved`. Keep the test scoped to Phase 2 routes only; do not extend into optimizer results or class editor routes.</action>
  <verify>
    <automated>npm run test:e2e -- tests/phase2-workflow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
- `playwright.config.ts` contains `webServer` with the exact command `npm run dev -- --host 127.0.0.1 --port 4173`.
- `tests/helpers/phase2SupabaseRoutes.ts` exists and contains route stubs for `**/auth/v1/user` and `**/rest/v1/rpc/replace_project_roster_state`.
- `tests/fixtures/phase2WorkflowData.ts` exists and contains the fixed project id `11111111-1111-4111-8111-111111111111`.
- `tests/phase2-workflow.spec.ts` exists.
- The spec file contains the exact test title `phase-2 workflow`.
- The spec file asserts the exact strings `All changes saved`, `Save failed`, and `Retry save`.
- The task-level smoke verify command `npm run test:e2e -- tests/phase2-workflow.spec.ts` passes.
  </acceptance_criteria>
  <done>The full setup-to-save browser journey has an executable smoke test that protects the Phase 2 contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the trust-signaling UI in the browser</name>
  <files>none - verification only</files>
  <read_first>
- src/pages/Configuration.tsx
- src/pages/PupilData.tsx
- tests/phase2-workflow.spec.ts
- .planning/phases/02-reliable-project-workflow/02-VALIDATION.md
  </read_first>
  <action>Run the automated Phase 2 suite first, then perform the five manual verification steps exactly as written so the trust-signaling UI states are confirmed by a human before Phase 2 closes.</action>
  <what-built>Configuration restore/retry UX, CSV mapping and failed-import reporting, and autosave status gating across the setup-to-save workflow.</what-built>
  <how-to-verify>
1. Start the app and open a real project.
2. On Configuration, verify a saved project shows stored values instead of defaults and that a forced load failure shows `Retry loading saved setup`.
3. On Pupil Data, import a CSV with one bad row and one ambiguous header, confirm the mapping modal appears, confirm the `Failed Imports` section lists the bad row, and confirm valid rows stay in the grid.
4. Edit a pupil row and verify the header transitions through `Saving...` and `All changes saved`.
5. Force or simulate one save failure and verify `Save failed` plus `Retry save`, while `Run Optimizer` stays disabled until the retry succeeds.
  </how-to-verify>
  <verify>
    <automated>npm run test -- src/pages/__tests__/workflow-reload.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- Human confirms all five verification steps completed.
- `npm run test -- src/pages/__tests__/workflow-reload.test.tsx` passes before requesting approval.
- Any reported issue references the exact step number that failed.
  </acceptance_criteria>
  <done>The user has either approved the end-to-end Phase 2 workflow or returned a specific failing verification step for follow-up.</done>
  <resume-signal>Type `approved` to close Phase 2, or reply with the failing step number and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
Run the integration regression suite, then the task-level Playwright smoke `npm run test:e2e -- tests/phase2-workflow.spec.ts`, then `npm run test:phase2` as the plan-level full gate before asking for human approval. Use the smaller `workflow-reload` test only for Task 3 feedback speed. Do not skip the blocking human verification task.
</verification>

<success_criteria>
All Phase 2 requirements have explicit regression coverage, and the user has manually confirmed the trust-building UI states that automation cannot judge well on its own.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliable-project-workflow/02-03-SUMMARY.md`
</output>
