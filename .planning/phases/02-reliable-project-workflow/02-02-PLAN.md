---
phase: 02-reliable-project-workflow
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/pupilWorkflow.ts
  - src/components/pupil/CsvMappingModal.tsx
  - src/components/pupil/IssuesPanel.tsx
  - src/pages/PupilData.tsx
  - src/pages/__tests__/pupil-import-validation.test.tsx
  - src/pages/__tests__/pupil-autosave.test.tsx
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - User can import a CSV, resolve unmatched headers, keep valid rows, and inspect failed rows immediately.
    - Inline cell errors and a persistent issues panel update as soon as a value becomes valid or invalid.
    - Pupil and chemistry changes autosave after a short idle delay and the optimizer stays disabled while data is saving or failed.
  artifacts:
    - path: src/lib/pupilWorkflow.ts
      provides: CSV normalization, mapping, validation, duplicate detection, and autosave helpers
    - path: src/components/pupil/CsvMappingModal.tsx
      provides: Header mapping modal for ambiguous CSV imports
    - path: src/components/pupil/IssuesPanel.tsx
      provides: Persistent blocking errors and warnings panel
    - path: src/pages/PupilData.tsx
      provides: Non-destructive import/edit/autosave workflow with save-status gating
  key_links:
    - from: src/pages/PupilData.tsx
      to: src/lib/pupilWorkflow.ts
      via: import parsing, issue generation, debounced autosave, and save gating
      pattern: buildImportPreview|collectPupilIssues|createRosterAutosave
    - from: src/pages/PupilData.tsx
      to: src/components/pupil/IssuesPanel.tsx
      via: errors and warnings props
      pattern: IssuesPanel
    - from: src/pages/PupilData.tsx
      to: src/components/project/WorkflowStatusHeader.tsx
      via: save state banner
      pattern: WorkflowStatusHeader
---

<objective>
Turn pupil entry into a resilient workflow with mapped CSV imports, live validation, a persistent issues panel, and debounced autosave that controls optimization readiness.

Purpose: Cover the second half of Phase 2 by removing fragile save timing and opaque import failures from the highest-volume screen in the product.
Output: A Pupil Data screen that imports partially-valid CSVs, keeps issue state visible, and autosaves roster edits safely before optimization.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-reliable-project-workflow/02-CONTEXT.md
@.planning/phases/02-reliable-project-workflow/02-RESEARCH.md
@.planning/phases/02-reliable-project-workflow/02-VALIDATION.md
@.planning/phases/02-reliable-project-workflow/02-01-PLAN.md
@src/pages/PupilData.tsx
@src/lib/api.ts
@src/lib/projectWorkflow.ts

<interfaces>
From src/lib/api.ts:
```typescript
export type Pupil = {
  id: string;
  name: string;
  gender: "Male" | "Female" | "Other";
  originSchool: string;
  needs: string;
  zone: string;
};

export type Chemistry = {
  positive: Array<[string, string]>;
  negative: Array<[string, string]>;
};

export async function saveProjectRosterState(
  projectId: string,
  pupils: Pupil[],
  chemistry: Chemistry
): Promise<void>;

export async function optimizeProject(
  projectId: string,
  signal?: AbortSignal
): Promise<OptimizeResponse>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add mapped CSV import, normalization, duplicate detection, and persistent issue reporting</name>
  <files>src/lib/pupilWorkflow.ts, src/components/pupil/CsvMappingModal.tsx, src/components/pupil/IssuesPanel.tsx, src/pages/PupilData.tsx, src/pages/__tests__/pupil-import-validation.test.tsx</files>
  <read_first>
- src/pages/PupilData.tsx
- src/lib/api.ts
- src/lib/projectWorkflow.ts
- .planning/phases/02-reliable-project-workflow/02-CONTEXT.md
- .planning/phases/02-reliable-project-workflow/02-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: CSV files with missing internal headers open a mapping modal instead of silently dropping rows.
    - Test 2: Valid rows import into the grid while invalid rows appear in a `Failed Imports` summary with row numbers and reasons.
    - Test 3: Gender inputs `M`, `male`, `boy`, and `man` normalize to `Male`; `F`, `female`, `girl`, and `woman` normalize to `Female`; any other non-empty value becomes `Other`.
    - Test 4: Duplicate rows with the same normalized `name|originSchool|gender|needs|zone` fingerprint are listed as blocking errors until the user edits one of them.
  </behavior>
  <action>Create `src/lib/pupilWorkflow.ts` to hold the page logic currently buried in `PupilData.tsx`. Export `EXPECTED_PUPIL_FIELDS = ["name", "originSchool", "gender", "needs", "zone"]`, `HEADER_ALIASES` with exact mappings `{ name: ["name", "student_name"], originSchool: ["origin_school", "originSchool"], gender: ["gender", "sex"], needs: ["needs", "academic_needs"], zone: ["zone", "location", "location_zone"] }`, `normalizeImportedGender(value: string)`, `buildImportPreview(rows, fieldMap)`, `collectPupilIssues(pupils, chemistry)`, and `summarizeFailedImports(...)`. Implement duplicate detection using the lowercase fingerprint `name|originSchool|gender|needs|zone`. Build `src/components/pupil/CsvMappingModal.tsx` as a modal with one `<select>` per expected field and exact action buttons `Cancel Import` and `Apply Mapping`. Build `src/components/pupil/IssuesPanel.tsx` with two sections titled exactly `Errors` and `Warnings`, and a failed import block titled exactly `Failed Imports`. Update `src/pages/PupilData.tsx` so CSV parsing still uses `papaparse`, but now inspects headers first: if any expected field lacks an alias match, open `CsvMappingModal`; otherwise apply the auto-map immediately. Import processing must append valid rows to the current grid instead of replacing it, list invalid rows in the failed-import summary, and keep duplicate rows visible in the grid with blocking issues rather than silently skipping them. Inline field inputs must show an error class after blur when their cell is invalid, and that class must disappear immediately after the value becomes valid. Add `src/pages/__tests__/pupil-import-validation.test.tsx` covering mapping modal display, partial import behavior, normalization rules, duplicate detection, issues panel rendering, and immediate issue clearing after edits.</action>
  <verify>
    <automated>npm run test -- src/pages/__tests__/pupil-import-validation.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/components/pupil/CsvMappingModal.tsx` contains the exact button labels `Cancel Import` and `Apply Mapping`.
- `src/components/pupil/IssuesPanel.tsx` contains the exact section labels `Errors`, `Warnings`, and `Failed Imports`.
- `src/lib/pupilWorkflow.ts` contains the exact alias keys `student_name`, `origin_school`, `originSchool`, `location`, and `location_zone`.
- `src/lib/pupilWorkflow.ts` contains the exact duplicate fingerprint string `name|originSchool|gender|needs|zone` in code or comments.
- `src/pages/__tests__/pupil-import-validation.test.tsx` exists and the verify command passes.
  </acceptance_criteria>
  <done>CSV import becomes recoverable instead of all-or-nothing, and users can see every blocking issue directly in the grid and issues panel.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add debounced autosave, save-state gating, and optimizer blocking rules to pupil workflow</name>
  <files>src/lib/pupilWorkflow.ts, src/pages/PupilData.tsx, src/pages/__tests__/pupil-autosave.test.tsx</files>
  <read_first>
- src/pages/PupilData.tsx
- src/lib/pupilWorkflow.ts
- src/lib/api.ts
- src/components/project/WorkflowStatusHeader.tsx
- .planning/phases/02-reliable-project-workflow/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Editing pupils or chemistry starts a debounced save after 2000 ms of inactivity.
    - Test 2: `Run Optimizer` is disabled while autosave is pending, while a save request is in flight, and after a failed save.
    - Test 3: A failed save keeps local edits on screen and surfaces `Save failed` plus a retry action.
    - Test 4: A successful save updates the banner to `All changes saved` without blocking continued editing or route navigation.
  </behavior>
  <action>Extend `src/lib/pupilWorkflow.ts` with `createRosterAutosave({ delayMs, onSave, onStateChange })` built on `lodash.debounce` and hard-code `delayMs: 2000` when it is used from `PupilData.tsx`. Update `src/pages/PupilData.tsx` to replace manual `saveToSupabase` timing with autosave-driven persistence. Keep direct DB reads for initial load, but after load the page must route all pupil and chemistry mutations through local state plus the debounced saver. Compute blocking issues from `collectPupilIssues` and do not send autosave writes while any blocking error exists; instead set `saveState` to the shared `blocked` status and let `WorkflowStatusHeader` render the exact label `Unsaved validation errors`. Render `WorkflowStatusHeader` above the table and feed it shared states from `WorkflowSaveState`, not page-specific ad hoc strings. Add an exact retry button label `Retry save` when the latest autosave fails. The `Run Optimizer` button must be disabled when there are zero pupils, when `saveState` is `saving`, when a debounced save is queued, when `saveState` is `error`, when `saveState` is `blocked`, or when `collectPupilIssues(...).errors.length > 0`. Preserve the existing 3-second optimizer loading delay, but only start it after pending autosave work is flushed successfully. Add `src/pages/__tests__/pupil-autosave.test.tsx` with fake timers to verify the 2000 ms debounce, queued-save disablement, blocked-validation banner behavior, failure-state retry flow, and successful-save banner transition.</action>
  <verify>
    <automated>npm run test -- src/pages/__tests__/pupil-autosave.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/pages/PupilData.tsx` imports `WorkflowStatusHeader`.
- `src/pages/PupilData.tsx` contains the exact labels `Unsaved validation errors` and `Retry save`.
- `src/pages/PupilData.tsx` references the shared `blocked` save state when validation errors are present.
- `src/pages/PupilData.tsx` contains a `disabled=` expression for `Run Optimizer` that references saving, queued save, save error, and blocking issues.
- `src/lib/pupilWorkflow.ts` contains `delayMs: 2000` or an equivalent exact `2000` debounce configuration used by `createRosterAutosave`.
- `src/pages/__tests__/pupil-autosave.test.tsx` exists and the verify command passes.
  </acceptance_criteria>
  <done>Pupil and chemistry edits persist predictably through a debounced save pipeline, and optimization cannot run against stale, invalid, or failed-save data.</done>
</task>

</tasks>

<verification>
Run the two targeted pupil workflow suites after implementation. The import suite should prove partial import plus issue reporting, and the autosave suite should prove the 2000 ms save gate and optimizer disablement logic.
</verification>

<success_criteria>
`FLOW-03` and `FLOW-04` are materially covered: CSV import is recoverable with clear feedback, issue state is always visible, and autosave governs when optimization is allowed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliable-project-workflow/02-02-SUMMARY.md`
</output>
