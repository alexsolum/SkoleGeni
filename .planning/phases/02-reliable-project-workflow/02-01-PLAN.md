---
phase: 02-reliable-project-workflow
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - playwright.config.ts
  - src/test/setup.ts
  - src/lib/projectWorkflow.ts
  - src/components/project/WorkflowStatusHeader.tsx
  - src/pages/Configuration.tsx
  - src/pages/__tests__/configuration-load.test.tsx
  - src/pages/__tests__/configuration-save.test.tsx
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
must_haves:
  truths:
    - User can reopen an existing project and see the saved configuration instead of default slider values.
    - User cannot leave configuration with invalid or failed-to-load constraint data.
    - Configuration save state is visible with exact loading, saving, saved, and failed messages.
  artifacts:
    - path: package.json
      provides: Phase 2 test and e2e scripts plus required dev/runtime dependencies
    - path: src/lib/projectWorkflow.ts
      provides: Shared configuration load/save/cache helpers for project workflow pages
    - path: src/pages/Configuration.tsx
      provides: Reliable configuration restore, validation gate, retry state, and navigation rules
  key_links:
    - from: src/pages/Configuration.tsx
      to: src/lib/projectWorkflow.ts
      via: loadProjectConstraints, saveProjectConstraints, readConfigurationCache, writeConfigurationCache
      pattern: loadProjectConstraints|saveProjectConstraints|readConfigurationCache|writeConfigurationCache
    - from: src/pages/Configuration.tsx
      to: src/components/project/WorkflowStatusHeader.tsx
      via: explicit status prop rendering
      pattern: WorkflowStatusHeader
---

<objective>
Establish the shared workflow contract and test harness, then make project configuration restore/save behavior reliable for reopen and continue flows.

Purpose: Cover the first half of Phase 2 by making saved setup data authoritative, blocking stale/default fallbacks, and creating the verification infrastructure later plans depend on.
Output: Working test harness, shared configuration workflow helpers, and a Configuration screen that restores, validates, saves, and retries predictably.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-reliable-project-workflow/02-CONTEXT.md
@.planning/phases/02-reliable-project-workflow/02-RESEARCH.md
@.planning/phases/02-reliable-project-workflow/02-VALIDATION.md
@.planning/phases/01-secure-data-foundation/01-01-SUMMARY.md
@src/pages/Welcome.tsx
@src/pages/Configuration.tsx
@src/lib/api.ts
@src/lib/supabase.ts
@package.json

<interfaces>
From src/lib/api.ts:
```typescript
export type OptimizerGenderPriority = "strict" | "flexible" | "ignore";
export type OptimizerOriginPriority = "strict" | "flexible" | "best_effort" | "ignore";
export type OptimizerLocationPriority = "strict" | "flexible" | "ignore" | "not_considered";
export type OptimizerNeedsPriority = "strict" | "flexible";

export type OptimizationConstraints = {
  minClassSize: number;
  maxClassSize: number;
  genderPriority: OptimizerGenderPriority;
  originPriority: OptimizerOriginPriority;
  locationPriority: OptimizerLocationPriority;
  needsPriority: OptimizerNeedsPriority;
};
```

From src/pages/Welcome.tsx:
```typescript
type ProjectRow = {
  id: string;
  title: string;
  updated_at: string | null;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 2 test harness and shared workflow status contract</name>
  <files>package.json, vitest.config.ts, playwright.config.ts, src/test/setup.ts, src/lib/projectWorkflow.ts, src/components/project/WorkflowStatusHeader.tsx</files>
  <read_first>
- package.json
- src/pages/Configuration.tsx
- src/pages/PupilData.tsx
- .planning/phases/02-reliable-project-workflow/02-CONTEXT.md
- .planning/phases/02-reliable-project-workflow/02-VALIDATION.md
  </read_first>
  <action>Add the missing Phase 2 verification foundation before feature work. In `package.json`, add runtime dependency `lodash.debounce` and dev dependencies `vitest`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `jsdom`, and `@playwright/test`. Add scripts with these exact keys: `"test": "vitest run"`, `"test:e2e": "playwright test"`, and `"test:phase2": "npm run test -- src/pages/__tests__/configuration-load.test.tsx src/pages/__tests__/configuration-save.test.tsx src/pages/__tests__/pupil-import-validation.test.tsx src/pages/__tests__/pupil-autosave.test.tsx src/pages/__tests__/workflow-reload.test.tsx && npm run test:e2e -- tests/phase2-workflow.spec.ts && npm run build"`. Create `vitest.config.ts` using `defineConfig`, `environment: "jsdom"`, `globals: true`, and `setupFiles: ["src/test/setup.ts"]`. Create `playwright.config.ts` with Chromium as the only project, `testDir: "./tests"`, and `use.baseURL` defaulting to `http://127.0.0.1:4173`. Create `src/test/setup.ts` importing `@testing-library/jest-dom/vitest` and running `afterEach(cleanup)`. Create `src/lib/projectWorkflow.ts` exporting `DEFAULT_CONSTRAINTS`, `type ConstraintFormState = OptimizationConstraints`, `type WorkflowSaveState = "idle" | "loading" | "saving" | "saved" | "error"`, `const CONFIG_CACHE_PREFIX = "skolegeni:config:"`, `readConfigurationCache(projectId: string)`, `writeConfigurationCache(projectId: string, form: ConstraintFormState)`, `loadProjectConstraints(projectId: string)`, `saveProjectConstraints(projectId: string, form: ConstraintFormState)`, and `validateConstraintForm(projectId: string | undefined, form: ConstraintFormState)`. `loadProjectConstraints` must read `project_constraints` from Supabase, return DB values when a row exists, and fall back to `DEFAULT_CONSTRAINTS` only when the row is absent; it must throw on query failure so the UI can block with retry. Create `src/components/project/WorkflowStatusHeader.tsx` rendering exact status labels `Loading saved setup...`, `Saving...`, `All changes saved`, and `Save failed` from a `state` prop.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
- `package.json` contains the exact script keys `"test"`, `"test:e2e"`, and `"test:phase2"`.
- `vitest.config.ts` contains `environment: "jsdom"` and `setupFiles: ["src/test/setup.ts"]`.
- `playwright.config.ts` contains `testDir: "./tests"` and `baseURL: process.env.PLAYWRIGHT_BASE_URL ?? "http://127.0.0.1:4173"`.
- `src/lib/projectWorkflow.ts` contains `CONFIG_CACHE_PREFIX = "skolegeni:config:"`.
- `src/components/project/WorkflowStatusHeader.tsx` contains all four exact labels: `Loading saved setup...`, `Saving...`, `All changes saved`, and `Save failed`.
  </acceptance_criteria>
  <done>Phase 2 has a runnable Vitest/Playwright scaffold and a shared configuration workflow module other plans can extend without rediscovering the contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make configuration restore, validation gating, and save behavior authoritative</name>
  <files>src/pages/Configuration.tsx, src/pages/__tests__/configuration-load.test.tsx, src/pages/__tests__/configuration-save.test.tsx</files>
  <read_first>
- src/pages/Configuration.tsx
- src/lib/projectWorkflow.ts
- src/components/project/WorkflowStatusHeader.tsx
- src/pages/Welcome.tsx
- .planning/phases/02-reliable-project-workflow/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Existing `project_constraints` row replaces default slider/radio values after load.
    - Test 2: Supabase load failure shows a blocking retry state and prevents navigation to pupil entry.
    - Test 3: Invalid constraints keep save blocked and surface the first validation error.
    - Test 4: Successful save writes cache, shows `All changes saved`, and navigates to `/pupils/:projectId`.
  </behavior>
  <action>Refactor `src/pages/Configuration.tsx` to use `projectWorkflow.ts` instead of owning persistence inline. On mount, if `projectId` is missing, keep the existing validation error. If `readConfigurationCache(projectId)` returns data, hydrate the form from cache immediately; then call `loadProjectConstraints(projectId)` and overwrite the form with DB values when a row exists, because DB values must beat defaults and stale cache for existing projects. While the fetch is running, render `WorkflowStatusHeader` with `Loading saved setup...`. If the fetch throws, replace the form with a blocking error card containing the exact button label `Retry loading saved setup` and do not render the save button as enabled. Remove the `Skip (MVP)` button entirely. Persist form changes to session storage with `writeConfigurationCache(projectId, form)` whenever the user changes a slider or radio selection. Keep validation rules explicit: `minClassSize` and `maxClassSize` must both be `>= 1`, `minClassSize` cannot exceed `maxClassSize`, and `needsPriority === "strict"` requires `maxClassSize >= 20`. On save, call `validateConstraintForm`, show the first error with `toast.error`, set the status header to `Saving...`, then call `saveProjectConstraints(projectId, form)`. On success, show `All changes saved`, keep the session cache in sync, and navigate to `/pupils/${projectId}`. On save failure, show `Save failed`, render a retry affordance, and keep the user on the page. Add `src/pages/__tests__/configuration-load.test.tsx` and `src/pages/__tests__/configuration-save.test.tsx` covering the four behaviors above with mocked Supabase responses and router navigation.</action>
  <verify>
    <automated>npm run test -- src/pages/__tests__/configuration-load.test.tsx src/pages/__tests__/configuration-save.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/pages/Configuration.tsx` no longer contains the string `Skip (MVP)`.
- `src/pages/Configuration.tsx` contains the exact button label `Retry loading saved setup`.
- `src/pages/Configuration.tsx` imports `WorkflowStatusHeader` from `src/components/project/WorkflowStatusHeader.tsx`.
- `src/pages/Configuration.tsx` calls `loadProjectConstraints(`, `saveProjectConstraints(`, `readConfigurationCache(`, and `writeConfigurationCache(`.
- `src/pages/__tests__/configuration-load.test.tsx` and `src/pages/__tests__/configuration-save.test.tsx` both exist and the verify command passes.
  </acceptance_criteria>
  <done>Existing projects reopen with saved configuration, invalid or failed loads are gated, and configuration save behavior is backed by automated tests.</done>
</task>

</tasks>

<verification>
Run the targeted configuration tests, then confirm `npm run build` stays green so the new harness and workflow module do not break the current app.
</verification>

<success_criteria>
`FLOW-01` and `FLOW-02` are materially covered: saved configuration becomes the source of truth for reopen flows, invalid configuration cannot be skipped, and the repo has the Phase 2 test harness needed by later plans.
</success_criteria>

<output>
After completion, create `.planning/phases/02-reliable-project-workflow/02-01-SUMMARY.md`
</output>
