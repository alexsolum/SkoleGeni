---
phase: 01-secure-data-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - README.md
  - docker-compose.yml
  - package.json
autonomous: true
requirements:
  - SECU-01
  - SECU-02
must_haves:
  truths:
    - "Local and production configuration no longer depend on unsafe committed defaults."
    - "The project documents the expected auth and env setup for the hardened foundation."
    - "The codebase is less likely to run with silent insecure defaults."
  artifacts:
    - path: ".env.example"
      provides: "Explicit env contract"
      min_lines: 3
    - path: "README.md"
      provides: "Updated setup instructions for the hardened model"
      min_lines: 40
    - path: "docker-compose.yml"
      provides: "Local-only credential/default handling"
      min_lines: 40
    - path: "package.json"
      provides: "Documented verification entry points if needed for safety checks"
      min_lines: 10
  key_links:
    - from: ".env.example"
      to: "README.md"
      via: "documented environment setup"
      pattern: "VITE_SUPABASE_URL|VITE_SUPABASE_ANON_KEY"
    - from: "docker-compose.yml"
      to: "README.md"
      via: "local development instructions"
      pattern: "docker compose|localhost"
---

<objective>
Remove unsafe credential/default handling and make the hardened foundation operable in local development.

Purpose: Support the security changes from Plan 01 without leaving the repo dependent on misleading or risky defaults.
Output: Cleaner env examples, updated local setup instructions, and safer documented defaults for development.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-data-foundation/01-CONTEXT.md
@.planning/phases/01-secure-data-foundation/01-RESEARCH.md
@.env.example
@README.md
@docker-compose.yml
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove or isolate unsafe local credential defaults and document the env contract</name>
  <files>.env.example, README.md, docker-compose.yml</files>
  <read_first>
    - .env.example
    - README.md
    - docker-compose.yml
    - .planning/phases/01-secure-data-foundation/01-CONTEXT.md
    - .planning/phases/01-secure-data-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Update `.env.example` so it clearly documents the required Supabase and optimizer environment variables for the hardened foundation. Update `README.md` to distinguish local-only development defaults from production/deployed configuration and to explain the authenticated ownership direction introduced in Phase 1. Update `docker-compose.yml` so any committed JWT-style or permissive credential defaults are clearly local-only and no longer presented as generic production-acceptable values; prefer obvious placeholder/local-dev wording over silently realistic-looking secrets.
  </action>
  <acceptance_criteria>
    - `.env.example` contains `VITE_SUPABASE_URL=`.
    - `.env.example` contains `VITE_SUPABASE_ANON_KEY=`.
    - `README.md` contains a section mentioning authenticated access, RLS, or owner-based access expectations.
    - `docker-compose.yml` no longer presents the current committed JWT-style anon token as an ordinary default without local-dev context.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "VITE_SUPABASE_URL=|VITE_SUPABASE_ANON_KEY=|authenticated|row level security|owner|local dev|anon" .env.example README.md docker-compose.yml</automated>
  </verify>
  <done>Local setup files and docs no longer normalize unsafe credential handling as an acceptable baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a lightweight safety verification entry point for Phase 1 foundation changes</name>
  <files>package.json, README.md</files>
  <read_first>
    - package.json
    - README.md
    - .planning/phases/01-secure-data-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the repo exposes at least one documented verification command for the hardened foundation.
    - Test 2: README tells future implementers how to run the safety check after Phase 1.
  </behavior>
  <action>
    Add a minimal script entry in `package.json` for a fast verification command that Phase 1 executors can use to confirm the frontend/tooling side still builds or checks cleanly after the hardening work. Update `README.md` to document that command in the local workflow. Keep this lightweight and compatible with the current stack; do not introduce a large new tooling surface in this plan.
  </action>
  <acceptance_criteria>
    - `package.json` contains at least one new script intended for Phase 1 verification.
    - `README.md` mentions the exact command from `package.json`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "\"[^\"]+\": \".+\"" package.json README.md</automated>
  </verify>
  <done>The repo has a documented, repeatable verification entry point for the hardened foundation work.</done>
</task>

</tasks>

<verification>
- Confirm docs and env examples reflect the new authenticated ownership baseline.
- Confirm future executors have a documented lightweight verification command.
</verification>

<success_criteria>
- Unsafe committed defaults are reduced or clearly isolated to local-only usage.
- Setup/docs reflect the hardened access model.
- The repo exposes a repeatable verification entry point for Phase 1 work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-data-foundation/01-03-SUMMARY.md`
</output>
