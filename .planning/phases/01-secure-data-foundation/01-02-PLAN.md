---
phase: 01-secure-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - supabase/migrations/0001_init.sql
  - src/pages/PupilData.tsx
  - src/lib/api.ts
  - api/optimizer.py
autonomous: true
requirements:
  - SECU-02
must_haves:
  truths:
    - "Saving pupil and chemistry changes no longer relies on destructive replace-all browser writes."
    - "Risky multi-step writes happen behind a trusted boundary."
    - "Optimizer execution reads from saved server-side project state rather than trusting a client-authoritative payload."
    - "The trusted save path is an explicit server-side or database-side component, not just a renamed client helper."
  artifacts:
    - path: "src/pages/PupilData.tsx"
      provides: "Updated save/optimize flow using trusted mutation path"
      min_lines: 200
    - path: "src/lib/api.ts"
      provides: "Typed client calls for server-side save and optimizer triggers"
      min_lines: 60
    - path: "supabase/migrations/0001_init.sql"
      provides: "Trusted roster-save RPC or equivalent database-side mutation contract"
      min_lines: 120
    - path: "api/optimizer.py"
      provides: "Trusted optimizer entry compatible with saved-state execution"
      min_lines: 250
  key_links:
    - from: "src/pages/PupilData.tsx"
      to: "src/lib/api.ts"
      via: "save and optimize calls"
      pattern: "save|optimi"
    - from: "supabase/migrations/0001_init.sql"
      to: "src/lib/api.ts"
      via: "explicit RPC or mutation contract for trusted roster saves"
      pattern: "create or replace function|rpc|replace_project"
    - from: "src/lib/api.ts"
      to: "api/optimizer.py"
      via: "HTTP request/response contract"
      pattern: "fetch|POST"
---

<objective>
Replace the destructive Phase 1 save path with a trusted mutation boundary and align optimizer execution to saved server-side state.

Purpose: Deliver the safety win the user asked for without moving the whole app behind a backend abstraction.
Output: Safer save orchestration, typed client helpers, and optimizer invocation based on persisted project data.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-data-foundation/01-CONTEXT.md
@.planning/phases/01-secure-data-foundation/01-RESEARCH.md
@src/pages/PupilData.tsx
@src/lib/api.ts
@api/optimizer.py
@supabase/migrations/0001_init.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace destructive roster saves with a trusted project-state mutation flow</name>
  <files>src/pages/PupilData.tsx, src/lib/api.ts, supabase/migrations/0001_init.sql</files>
  <read_first>
    - src/pages/PupilData.tsx
    - src/lib/api.ts
    - supabase/migrations/0001_init.sql
    - .planning/phases/01-secure-data-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: the client no longer executes `supabase.from("pupils").delete().eq("project_id", projectId)` during normal saves.
    - Test 2: the client no longer executes `supabase.from("chemistry_links").delete().eq("project_id", projectId)` during normal saves.
    - Test 3: the save path uses a dedicated helper in `src/lib/api.ts` for the risky multi-step mutation.
  </behavior>
  <action>
    Refactor `src/pages/PupilData.tsx` so the current multi-step roster save no longer deletes all pupils and chemistry links from the browser before reinserting them. Define an explicit trusted mutation boundary in `supabase/migrations/0001_init.sql` as a database-side function/RPC that replaces a project's roster state transactionally after ownership checks, or an equivalently explicit trusted server-side contract if the planner chooses that path. Introduce typed helper(s) in `src/lib/api.ts` that call this trusted mutation boundary by name, and update `src/pages/PupilData.tsx` to use that helper while preserving the current page UX and state shape.
  </action>
  <acceptance_criteria>
    - `src/pages/PupilData.tsx` no longer contains `.from("pupils").delete().eq("project_id", projectId)`.
    - `src/pages/PupilData.tsx` no longer contains `.from("chemistry_links").delete().eq("project_id", projectId)`.
    - `src/lib/api.ts` contains an exported helper specifically for saving project roster state.
    - `supabase/migrations/0001_init.sql` contains a named trusted mutation function or RPC for replacing project roster state.
    - `src/pages/PupilData.tsx` uses the new helper from `src/lib/api.ts`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "delete\\(\\)\\.eq\\(\"project_id\", projectId\\)|save.*project|save.*roster|create or replace function|rpc|replace_project" src/pages/PupilData.tsx src/lib/api.ts supabase/migrations/0001_init.sql</automated>
  </verify>
  <done>The client no longer performs destructive full-project roster replacement directly from the browser, and the replacement save flow is backed by an explicit trusted mutation component.</done>
</task>

<task type="auto">
  <name>Task 2: Change optimizer execution to use saved server-side project state</name>
  <files>src/pages/PupilData.tsx, src/lib/api.ts, api/optimizer.py</files>
  <read_first>
    - src/pages/PupilData.tsx
    - src/lib/api.ts
    - api/optimizer.py
    - .planning/phases/01-secure-data-foundation/01-CONTEXT.md
    - .planning/phases/01-secure-data-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Update the optimization trigger so the browser no longer treats the full client-assembled payload as the authoritative source. Add a typed API helper in `src/lib/api.ts` for triggering optimization by `projectId` after successful saved-state persistence. Adjust `api/optimizer.py` so it can support a trusted saved-state execution path keyed by project identity instead of requiring the browser to send the complete authoritative roster payload. Keep the current scoring/response shape compatible enough that later phases can build on it without a second Phase 1 rewrite.
  </action>
  <acceptance_criteria>
    - `src/pages/PupilData.tsx` does not call `optimizeClasses(payload)` with a full inline assembled roster payload as the only execution path.
    - `src/lib/api.ts` exports a project-id based optimizer trigger helper.
    - `api/optimizer.py` contains a project-id based execution path or equivalent trusted saved-state entry point.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "optimizeClasses\\(|projectId|saved state|trigger" src/pages/PupilData.tsx src/lib/api.ts api/optimizer.py</automated>
  </verify>
  <done>Optimizer execution is aligned with saved server-side state rather than a purely client-authoritative payload.</done>
</task>

</tasks>

<verification>
- Confirm the destructive browser save flow is gone.
- Confirm the optimizer can be triggered from persisted project state after a successful save.
</verification>

<success_criteria>
- Risky project-state writes happen behind a trusted boundary.
- The optimizer is triggered from saved authoritative project state.
- The browser retains only the low-risk/simple write responsibilities approved in context.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-data-foundation/01-02-SUMMARY.md`
</output>
