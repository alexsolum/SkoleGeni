---
phase: 01-secure-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0001_init.sql
  - docker/db/init/0001_schema.sql
  - docker/db/init/0000_roles.sql
  - src/lib/supabase.ts
  - src/pages/Welcome.tsx
autonomous: true
requirements:
  - SECU-01
must_haves:
  truths:
    - "An authenticated owner can access only their own project-scoped data."
    - "Anonymous users no longer have broad CRUD access to roster data."
    - "Project ownership exists in the schema and is used by access rules."
    - "Unauthenticated users do not load or create project data from the app shell."
  artifacts:
    - path: "supabase/migrations/0001_init.sql"
      provides: "Owner-aware schema and RLS policies"
      min_lines: 80
    - path: "docker/db/init/0001_schema.sql"
      provides: "Local database bootstrap aligned with the canonical schema"
      min_lines: 80
    - path: "src/lib/supabase.ts"
      provides: "Safer authenticated client initialization and env failure behavior"
      min_lines: 8
    - path: "src/pages/Welcome.tsx"
      provides: "Authenticated project-entry boundary for listing and creating projects"
      min_lines: 40
  key_links:
    - from: "supabase/migrations/0001_init.sql"
      to: "public.projects.owner_user_id and related RLS policies"
      via: "owner-based access checks"
      pattern: "owner_user_id|create policy|enable row level security"
    - from: "docker/db/init/0001_schema.sql"
      to: "supabase/migrations/0001_init.sql"
      via: "matching schema and policy definitions"
      pattern: "owner_user_id|create policy|enable row level security"
    - from: "src/lib/supabase.ts"
      to: "src/pages/Welcome.tsx"
      via: "session-aware project access and authenticated project creation"
      pattern: "getSession|auth|owner"
---

<objective>
Establish project ownership and access control as the foundation for all Phase 1 hardening work.

Purpose: Replace the MVP-wide open data model with owner-based authenticated access while keeping the browser-read pattern the user approved.
Output: Updated canonical and local SQL schema, owner-aware policies, and safer client auth/env handling.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-data-foundation/01-CONTEXT.md
@.planning/phases/01-secure-data-foundation/01-RESEARCH.md
@supabase/migrations/0001_init.sql
@docker/db/init/0000_roles.sql
@docker/db/init/0001_schema.sql
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ownership and owner-scoped RLS to the canonical and local database schema</name>
  <files>supabase/migrations/0001_init.sql, docker/db/init/0001_schema.sql, docker/db/init/0000_roles.sql</files>
  <read_first>
    - supabase/migrations/0001_init.sql
    - docker/db/init/0001_schema.sql
    - docker/db/init/0000_roles.sql
    - .planning/phases/01-secure-data-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: schema defines `owner_user_id uuid` on `public.projects` and all project-scoped access can be reduced to owner membership.
    - Test 2: RLS is enabled on `projects`, `project_constraints`, `pupils`, `chemistry_links`, and `optimization_runs`.
    - Test 3: anon broad CRUD grants are removed or narrowed so owner-based authenticated access is the live behavior.
  </behavior>
  <action>
    Update `supabase/migrations/0001_init.sql` to add `owner_user_id uuid not null` to `public.projects`, using a pragmatic single-owner Phase 1 model that leaves room for later collaboration. Replace the current `disable row level security` statements with `enable row level security` on all project-scoped tables. Add explicit owner-based policies so authenticated users can only select, insert, update, and delete rows for projects they own; for child tables (`project_constraints`, `pupils`, `chemistry_links`, `optimization_runs`) the policies must reference the owning `projects` row by `project_id`. Remove the current blanket grants that allow `anon` broad CRUD access. Mirror the same ownership column, RLS, and grant/policy intent into `docker/db/init/0001_schema.sql`, and adjust `docker/db/init/0000_roles.sql` if needed so the local bootstrap still supports authenticated owner-based access patterns.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/0001_init.sql` contains `owner_user_id uuid`.
    - `supabase/migrations/0001_init.sql` contains `alter table public.projects enable row level security;`.
    - `supabase/migrations/0001_init.sql` contains owner-based `create policy` statements for all five project-scoped tables.
    - `supabase/migrations/0001_init.sql` no longer contains `grant select, insert, update, delete on table public.projects to anon, authenticated;`.
    - `docker/db/init/0001_schema.sql` contains `owner_user_id uuid`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "owner_user_id|enable row level security|create policy|grant select, insert, update, delete" supabase/migrations/0001_init.sql docker/db/init/0001_schema.sql docker/db/init/0000_roles.sql</automated>
  </verify>
  <done>Canonical and local schema files define owner-based authenticated access and no longer rely on the MVP-wide open CRUD model.</done>
</task>

<task type="auto">
  <name>Task 2: Make the browser client fail safely and gate project entry on authenticated ownership</name>
  <files>src/lib/supabase.ts, src/pages/Welcome.tsx</files>
  <read_first>
    - src/lib/supabase.ts
    - src/pages/Welcome.tsx
    - .planning/phases/01-secure-data-foundation/01-CONTEXT.md
    - .planning/phases/01-secure-data-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Update `src/lib/supabase.ts` so it no longer silently creates a client with empty strings when `VITE_SUPABASE_URL` or `VITE_SUPABASE_ANON_KEY` is missing. Keep the browser-read pattern, but make misconfiguration explicit and safer by throwing a descriptive error in development/runtime initialization instead of proceeding with an unusable client. Then update `src/pages/Welcome.tsx` so unauthenticated users do not list or create projects from the app shell: require a valid authenticated session before loading project rows, and ensure new project creation runs in an authenticated owner context compatible with the `owner_user_id` model introduced in the schema. Preserve the current route structure and existing `supabase` export shape.
  </action>
  <acceptance_criteria>
    - `src/lib/supabase.ts` does not contain `createClient(supabaseUrl ?? "", supabaseAnonKey ?? "")`.
    - `src/lib/supabase.ts` contains a thrown error mentioning both `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
    - `src/lib/supabase.ts` still exports `supabase`.
    - `src/pages/Welcome.tsx` contains an authenticated session check before project loading or creation.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "throw new Error|VITE_SUPABASE_URL|VITE_SUPABASE_ANON_KEY|export const supabase|getSession|auth" src/lib/supabase.ts src/pages/Welcome.tsx</automated>
  </verify>
  <done>The app no longer initializes a fake empty Supabase client under missing env conditions, and unauthenticated users do not reach project-scoped data flows from the welcome screen.</done>
</task>

</tasks>

<verification>
- Confirm the canonical migration and local bootstrap define the same ownership and RLS intent.
- Confirm the browser client still exports a usable `supabase` client path for authenticated reads.
- Confirm the welcome flow now treats authenticated ownership as the boundary for project access.
</verification>

<success_criteria>
- Owner-based authenticated access rules are present in schema files.
- Broad anonymous CRUD access is removed from the current MVP foundation.
- Browser-side data access can still be used for reads under an authenticated/RLS model.
- Unauthenticated users do not load or create project-scoped data from the app shell.
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-data-foundation/01-01-SUMMARY.md`
</output>
