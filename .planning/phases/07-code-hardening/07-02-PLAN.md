---
phase: 07-code-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - docker/optimizer.Dockerfile
  - .planning/phases/07-code-hardening/07-VALIDATION.md
autonomous: true
requirements:
  - HARD-01
  - HARD-02
must_haves:
  truths:
    - "With required env vars injected, the optimizer container starts successfully and serves HTTP traffic on the exact `PORT` value provided at runtime."
    - "Without required env vars, the container stays up in a not-ready state so `GET /health` can report missing-env detail with a non-200 response."
    - "Startup and failure logs are flushed promptly to container stdout/stderr so Cloud Run can surface them during deploys and crashes."
    - "Phase 7 validation artifacts name the exact automated commands that prove the runtime contract above."
  artifacts:
    - path: "docker/optimizer.Dockerfile"
      provides: "Cloud-ready optimizer runtime with unbuffered logging, readiness-visible missing-env behavior, and PORT-aware startup"
      contains: "PYTHONUNBUFFERED=1"
    - path: ".planning/phases/07-code-hardening/07-VALIDATION.md"
      provides: "Closed Nyquist validation contract for Phase 7"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "docker/optimizer.Dockerfile"
      to: "api.optimizer:app"
      via: "uvicorn startup command"
      pattern: "api.optimizer:app"
    - from: ".planning/phases/07-code-hardening/07-VALIDATION.md"
      to: "api/test_hardening.py"
      via: "per-task verification map"
      pattern: "api/test_hardening.py"
---

<objective>
Finish the runtime hardening and validation closure required to treat the optimizer container as Phase-7-ready for Cloud Run work.

Purpose: convert the newly hardened API surface into a cloud-ready container contract and ship the phase with complete verification evidence.
Output: a PORT-aware, unbuffered optimizer Dockerfile and an updated `07-VALIDATION.md` that reflects the implemented hardening test coverage.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-code-hardening/07-CONTEXT.md
@.planning/phases/07-code-hardening/07-RESEARCH.md
@.planning/phases/07-code-hardening/07-VALIDATION.md
@.planning/phases/07-code-hardening/07-01-PLAN.md
@.planning/codebase/STACK.md
@docker/optimizer.Dockerfile
@api/optimizer.py
@api/test_hardening.py
@requirements.txt

<interfaces>
Expected runtime target from `api/optimizer.py`:
```python
app = FastAPI(...)
```

Current Docker entrypoint target:
```dockerfile
CMD ["python", "-m", "uvicorn", "api.optimizer:app", "--host", "0.0.0.0", "--port", "8000"]
```

Current phase validation quick commands:
```text
pytest api/test_hardening.py -q
pytest api -q && npm run test
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the optimizer image Cloud Run compatible and unbuffered</name>
  <files>docker/optimizer.Dockerfile</files>
  <read_first>
    docker/optimizer.Dockerfile
    api/optimizer.py
    .planning/phases/07-code-hardening/07-CONTEXT.md
    .planning/phases/07-code-hardening/07-RESEARCH.md
    .planning/ROADMAP.md
  </read_first>
  <acceptance_criteria>
    - `docker/optimizer.Dockerfile` contains the exact line `ENV PYTHONUNBUFFERED=1`.
    - `docker/optimizer.Dockerfile` still starts `api.optimizer:app` with uvicorn.
    - The startup command references `${PORT:-8000}` or an equivalent runtime expression so Cloud Run can inject the listen port.
    - `docker/optimizer.Dockerfile` still binds host `0.0.0.0`.
    - `docker build -f docker/optimizer.Dockerfile .` exits 0.
    - `rg -n "PYTHONUNBUFFERED=1|PORT:-8000|api.optimizer:app" docker/optimizer.Dockerfile` exits 0.
  </acceptance_criteria>
  <action>Update `docker/optimizer.Dockerfile` to harden the runtime for Cloud Run. Add `ENV PYTHONUNBUFFERED=1` before the final command so Python stdout and stderr are not buffered. Replace the fixed-port JSON-array `CMD` with a shell form that still launches `python -m uvicorn api.optimizer:app --host 0.0.0.0` but reads the listen port from `PORT` with a fallback of `8000`; for example, use `/bin/sh -c` and `${PORT:-8000}`. Keep the base image, workdir, requirements install, and `COPY api ./api` steps unchanged. Do not add new system packages or deployment-specific secrets here. Task-level verification should stay fast: prove the Dockerfile contains the unbuffered runtime and `PORT`-aware startup contract, and confirm the image builds. Defer the full container run sequence to the plan-level verification gate so the expensive runtime checks happen once per wave instead of after this task commit.</action>
  <verify>
    <automated>powershell -NoProfile -Command "docker build -t skolegeni-optimizer-phase7 -f docker/optimizer.Dockerfile . | Out-Null; rg -n 'PYTHONUNBUFFERED=1|PORT:-8000|api.optimizer:app' docker/optimizer.Dockerfile"</automated>
  </verify>
  <done>The optimizer image now emits logs immediately and can listen on the port Cloud Run assigns instead of assuming a fixed local-only port.</done>
</task>

<task type="auto">
  <name>Task 2: Close the Phase 7 validation gaps with concrete hardening coverage</name>
  <files>.planning/phases/07-code-hardening/07-VALIDATION.md</files>
  <read_first>
    .planning/phases/07-code-hardening/07-VALIDATION.md
    .planning/phases/07-code-hardening/07-01-PLAN.md
    api/test_hardening.py
    api/test_optimizer.py
    api/test_feasibility.py
    docker/optimizer.Dockerfile
    .planning/phases/05-polish-and-release-readiness/05-03-SUMMARY.md
    .planning/RETROSPECTIVE.md
  </read_first>
  <acceptance_criteria>
    - `.planning/phases/07-code-hardening/07-VALIDATION.md` frontmatter contains `nyquist_compliant: true`.
    - `.planning/phases/07-code-hardening/07-VALIDATION.md` frontmatter contains `wave_0_complete: true`.
    - The Per-Task Verification Map includes concrete commands for `07-01-01`, `07-01-02`, `07-01-03`, and `07-02-01`.
    - The Wave 0 Requirements section no longer contains unchecked bullets.
    - The file contains both `python -m pytest api/test_hardening.py -q` and `python -m pytest api -q`.
    - The file contains a command-level assertion for `PYTHONUNBUFFERED=1`, plus a real `docker run` verification that checks the injected `PORT` path and the missing-env not-ready health path.
  </acceptance_criteria>
  <action>Update `.planning/phases/07-code-hardening/07-VALIDATION.md` so it reflects the implemented Phase 7 work instead of draft placeholders. Set `nyquist_compliant: true` and `wave_0_complete: true`. Replace the current `❌ W0` gaps with the actual verification paths created in Plan 01 and this plan: `python -m pytest api/test_hardening.py -q -k cors`, `python -m pytest api/test_hardening.py -q -k health`, `python -m pytest api/test_hardening.py -q -k "root or startup"`, and `python -m pytest api -q`. Add a concrete runtime verification line for the Dockerfile hardening that proves more than a successful image build: include a `docker run` check that injects `PORT` plus the required Supabase env vars and confirms the service responds on that port, and a second `docker run` check that confirms the container stays up with `/health` returning structured non-200 missing-env detail when the required env vars are absent. Keep a command-level assertion for `PYTHONUNBUFFERED=1` as well. Keep the manual verification section focused on deployed Cloud Run and browser-origin checks only; do not add new deferred observability or preview-origin tasks.</action>
  <verify>
    <automated>rg -n "nyquist_compliant: true|wave_0_complete: true|python -m pytest api/test_hardening.py -q|python -m pytest api -q|PYTHONUNBUFFERED=1|PORT:-8000|docker run|18080|api.optimizer:app" .planning/phases/07-code-hardening/07-VALIDATION.md docker/optimizer.Dockerfile</automated>
  </verify>
  <done>Phase 7 ships with complete, concrete verification guidance rather than draft placeholders or missing Wave 0 coverage.</done>
</task>

</tasks>

<verification>
Build the optimizer image after the Dockerfile change, then run one container with `PORT=18080`, `SUPABASE_URL=https://example.supabase.co`, and `SUPABASE_ANON_KEY=test-anon-key` and confirm `http://127.0.0.1:18080/health` returns HTTP 200. Run a second container without the required Supabase env vars and confirm the process stays up long enough for `http://127.0.0.1:18080/health` to return a structured non-200 response naming `missing_env`. After the runtime checks pass, confirm the validation doc names the exact automated commands that prove CORS, readiness, route hardening, and runtime configuration.
</verification>

<success_criteria>
`docker/optimizer.Dockerfile` sets `PYTHONUNBUFFERED=1` and honors `PORT`, the image builds successfully, and `07-VALIDATION.md` records the final hardening verification contract with no missing Wave 0 gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-hardening/07-02-SUMMARY.md`
</output>
