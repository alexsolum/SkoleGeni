---
phase: 07-code-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - api/test_hardening.py
  - api/optimizer.py
  - api/test_optimizer.py
  - api/test_feasibility.py
autonomous: true
requirements:
  - HARD-01
  - HARD-02
must_haves:
  truths:
    - "A browser preflight from any origin receives CORS headers that allow the existing bearer-auth `/project` flow."
    - "When required runtime env vars are missing, the FastAPI app stays running, logs a not-ready startup summary, and `GET /health` returns a structured non-200 response that names the missing env groups."
    - "With required env vars present, startup logs a concise readiness summary and `GET /health` returns readiness JSON with HTTP 200 only when OR-Tools readiness also passes."
    - "`GET /` returns a safe informational response instead of exposing optimization behavior."
    - "The public unauthenticated `POST /` optimization entry point no longer exists."
  artifacts:
    - path: "api/test_hardening.py"
      provides: "Regression coverage for CORS, startup readiness logging, degraded missing-env health reporting, safe root behavior, and removed root POST surface"
      contains: "def test_cors_preflight_allows_authorization_header"
    - path: "api/optimizer.py"
      provides: "FastAPI hardening via CORSMiddleware, startup readiness validation, moderate logging, readiness state, and safe public routes"
      contains: "CORSMiddleware"
    - path: "api/test_optimizer.py"
      provides: "Optimizer diagnostics coverage that no longer depends on public POST /"
      contains: "_optimize_request"
    - path: "api/test_feasibility.py"
      provides: "Feasibility regression coverage aligned with the hardened API contract"
      contains: "_optimize_request"
  key_links:
    - from: "src/lib/api.ts"
      to: "api/optimizer.py"
      via: "POST /project bearer-auth fetch flow"
      pattern: "fetch\\(url.*POST.*project"
    - from: "api/optimizer.py"
      to: "api/test_hardening.py"
      via: "preflight, startup, and readiness assertions"
      pattern: "health|CORS|OPTIONS|caplog|startup"
    - from: "api/optimizer.py"
      to: "api/test_optimizer.py"
      via: "shared _optimize_request contract"
      pattern: "_optimize_request"
---

<objective>
Harden the FastAPI optimizer surface so the cloud-deployed service exposes only safe public routes, answers browser preflight correctly, and reports real readiness.

Purpose: satisfy the Phase 7 service-boundary requirements before any container or Cloud Run deployment work begins.
Output: a new `api/test_hardening.py` contract, a hardened `api/optimizer.py`, and existing Python solver tests updated away from the removed public `POST /` route.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-code-hardening/07-CONTEXT.md
@.planning/phases/07-code-hardening/07-RESEARCH.md
@.planning/phases/07-code-hardening/07-VALIDATION.md
@.planning/phases/03-trustworthy-optimization-results/03-01-SUMMARY.md
@.planning/phases/05-polish-and-release-readiness/05-03-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/CONVENTIONS.md
@api/optimizer.py
@api/test_optimizer.py
@api/test_feasibility.py
@src/lib/api.ts

<interfaces>
From `src/lib/api.ts`:
```typescript
export async function optimizeProject(projectId: string, signal?: AbortSignal): Promise<OptimizeResponse>;
export async function scoreProjectAssignment(payload: ScoreProjectAssignmentRequest, signal?: AbortSignal): Promise<Score>;
```

Current public optimizer routes in `api/optimizer.py`:
```python
@app.post("/")
def optimize(req: OptimizeRequest) -> OptimizeResponse: ...

@app.post("/project")
def optimize_project(req: OptimizeProjectRequest, request: Request) -> OptimizeResponse: ...

@app.post("/project/score")
def score_project_assignment(req: ScoreProjectAssignmentRequest, request: Request) -> Score: ...
```

Current env lookup helpers in `api/optimizer.py`:
```python
def _supabase_url() -> str: ...
def _supabase_anon_key() -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing hardening contract for CORS, readiness, and safe root routes</name>
  <files>api/test_hardening.py</files>
  <read_first>
    api/test_hardening.py
    api/optimizer.py
    api/test_optimizer.py
    api/test_feasibility.py
    .planning/phases/07-code-hardening/07-CONTEXT.md
    .planning/phases/07-code-hardening/07-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `OPTIONS /project` with `Origin: https://preview.example.vercel.app` and `Access-Control-Request-Headers: authorization,content-type` returns success plus reflected allow-origin and explicit allow-headers/methods.
    - Test 2: with required env vars present, startup emits a concise readiness summary log and `GET /health` returns HTTP 200 with readiness JSON when the OR-Tools module is loaded.
    - Test 3: with required env vars absent, FastAPI startup completes in a not-ready state, logs the missing env groups, and `GET /health` returns a structured non-200 response with missing-env detail.
    - Test 4: `GET /` returns an informational JSON payload and `POST /` is not a working unauthenticated optimization route.
  </behavior>
  <acceptance_criteria>
    - `api/test_hardening.py` exists.
    - `api/test_hardening.py` contains the exact test names `test_cors_preflight_allows_authorization_header`, `test_health_reports_ready_when_env_present`, `test_health_reports_missing_env_when_required_env_missing`, and `test_root_get_is_safe_and_root_post_is_not_exposed`.
    - The file uses `with TestClient(app) as client:` so lifespan execution is exercised.
    - The CORS test asserts `access-control-allow-origin` and `access-control-allow-headers`.
    - The ready-health test asserts startup log output through `caplog`.
    - The missing-env health test asserts startup does not raise, `/health` returns non-200 JSON with `missing_env`, and log output includes the missing env group names.
    - `python -m pytest api/test_hardening.py -q` fails before the production code change.
  </acceptance_criteria>
  <action>Create `api/test_hardening.py` as a focused pytest module for Phase 7. Use `TestClient` as a context manager in every request-serving test so FastAPI lifespan runs. Cover four cases with exact assertions: a preflight `OPTIONS /project` request from `https://preview.example.vercel.app` with requested headers `authorization,content-type`; a ready startup plus `GET /health` path with `SUPABASE_URL=https://example.supabase.co` and `SUPABASE_ANON_KEY=test-anon-key`; a missing-env startup path with both env groups unset where startup completes but `/health` returns a structured non-200 JSON body naming the missing env groups; and a safe root-path contract where `GET /` returns informational JSON and `POST /` does not expose optimization. Use `caplog` to assert moderate logging behavior in both startup paths: a concise readiness summary when ready, and a clear not-ready missing-env summary when config is incomplete. Keep the tests self-contained with `monkeypatch` for env control. Do not stub CORS manually, do not assert startup failure when env is missing, and do not add Cloud Run-only network tests here.</action>
  <verify>
    <automated>python -m pytest api/test_hardening.py -q</automated>
  </verify>
  <done>The failing Phase 7 contract exists and exercises the exact service-hardening behaviors the implementation must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement FastAPI hardening with middleware-based CORS and readiness-backed routes</name>
  <files>api/optimizer.py</files>
  <read_first>
    api/optimizer.py
    api/test_hardening.py
    src/lib/api.ts
    .planning/phases/07-code-hardening/07-CONTEXT.md
    .planning/phases/07-code-hardening/07-RESEARCH.md
    .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Browser preflight to `/project` succeeds without changing the existing bearer-auth client contract in `src/lib/api.ts`.
    - Missing required Supabase env values keep the app in a not-ready state while still allowing `GET /health` to report structured failure detail.
    - Startup emits moderate logging: one concise readiness summary on success, and a clear not-ready startup summary when required env is missing.
    - `GET /` is safe for accidental hits and `POST /` no longer performs optimization.
  </behavior>
  <acceptance_criteria>
    - `api/optimizer.py` contains `CORSMiddleware`.
    - `api/optimizer.py` contains `allow_origin_regex=".*"`.
    - `api/optimizer.py` contains explicit `allow_methods` and explicit `allow_headers` entries including `Authorization` and `Content-Type`.
    - `api/optimizer.py` contains module-level logger setup plus at least one startup summary log for the ready path and one clear startup log for the missing-env not-ready path.
    - `api/optimizer.py` defines `@app.get("/")`.
    - `api/optimizer.py` defines `@app.get("/health")`.
    - `api/optimizer.py` does not contain `@app.post("/")`.
    - `api/optimizer.py` contains a readiness payload key named `missing_env`.
    - `python -m pytest api/test_hardening.py -q` exits 0 after the change.
  </acceptance_criteria>
  <action>Refactor `api/optimizer.py` to harden the app surface in place. Add FastAPI lifespan initialization that computes a readiness payload once at startup with these concrete fields: `status`, `service`, `ortools_ready`, `missing_env`, and `checked_at` or an equivalent deterministic readiness timestamp/string. Treat `SUPABASE_URL|VITE_SUPABASE_URL` and `SUPABASE_ANON_KEY|VITE_SUPABASE_ANON_KEY` as the required env groups, collect whichever are missing into `missing_env`, and keep the app running even when any are absent so probes can inspect it; in that state log a clear not-ready startup summary and have `GET /health` return a structured non-200 response naming the missing env groups. When startup succeeds, log one concise readiness summary at normal log level; keep normal request/error logging only and avoid verbose per-request debug noise. Add `CORSMiddleware` with `allow_origin_regex=\".*\"`, `allow_credentials=True`, a broad explicit method list that includes at least `GET`, `POST`, and `OPTIONS`, and a permissive explicit header list that includes at least `Authorization`, `Content-Type`, and `Accept`. Add a small `GET /` response that names the service and points callers to `POST /project`. Add a readiness-oriented `GET /health` that returns HTTP 200 only when startup validation and OR-Tools readiness are both true. Keep `POST /project` and `POST /project/score` intact, preserve existing optimizer data models, and remove the public unauthenticated `POST /` route entirely instead of protecting it with a shared secret.</action>
  <verify>
    <automated>python -m pytest api/test_hardening.py -q</automated>
  </verify>
  <done>The optimizer exposes only safe public GET routes plus authenticated project routes, and the new hardening contract passes.</done>
</task>

<task type="auto">
  <name>Task 3: Realign existing Python regression tests with the removed public root POST route</name>
  <files>api/test_optimizer.py, api/test_feasibility.py</files>
  <read_first>
    api/test_optimizer.py
    api/test_feasibility.py
    api/optimizer.py
    api/test_hardening.py
    .planning/phases/03-trustworthy-optimization-results/03-01-SUMMARY.md
    .planning/phases/05-polish-and-release-readiness/05-03-SUMMARY.md
  </read_first>
  <acceptance_criteria>
    - `api/test_optimizer.py` does not contain `client.post("/")`.
    - `api/test_feasibility.py` does not contain `client.post("/")`.
    - Both files import and use `_optimize_request` or another non-public-root contract from `api/optimizer.py` instead of relying on the removed route.
    - `python -m pytest api/test_optimizer.py api/test_feasibility.py -q` exits 0.
    - `python -m pytest api/test_hardening.py api/test_optimizer.py api/test_feasibility.py -q` exits 0.
  </acceptance_criteria>
  <action>Update `api/test_optimizer.py` and `api/test_feasibility.py` so they still exercise solver and diagnostic behavior after `POST /` is removed. Reuse the internal `_optimize_request` contract from `api/optimizer.py` rather than inventing a new testing-only route. Keep each file’s current assertion intent: `api/test_optimizer.py` should still verify structured infeasibility diagnostics for impossible class-size inputs, and `api/test_feasibility.py` should still cover conflicting bounds, zero-pupil behavior, single-class success, under-minimum rosters, and exact-fit class counts. Do not reintroduce a public optimization route just to satisfy old tests.</action>
  <verify>
    <automated>python -m pytest api/test_hardening.py api/test_optimizer.py api/test_feasibility.py -q</automated>
  </verify>
  <done>Existing optimizer regression coverage remains green without depending on a public unauthenticated root optimization endpoint.</done>
</task>

</tasks>

<verification>
Run the new hardening contract first, implement until it passes, then rerun the updated solver regression files so the route-surface hardening does not silently break diagnostic coverage.
</verification>

<success_criteria>
`api/test_hardening.py` passes, `api/optimizer.py` answers CORS preflight and readiness correctly, `GET /` is safe, `POST /` is gone, and the existing Python regression suite no longer depends on the removed root optimization route.
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-hardening/07-01-SUMMARY.md`
</output>
