---
phase: 03-trustworthy-optimization-results
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/pages/Results.tsx, src/components/ClassCard.tsx, src/components/ClassCard.test.tsx]
autonomous: true
requirements: [OPTI-02]
must_haves:
  truths:
    - "User sees real pupil names and distribution charts on class cards"
    - "Unit tests verify that Pupil IDs are correctly mapped to names"
    - "Unit tests verify that charts receive correct distribution data"
  artifacts:
    - path: "src/components/ClassCard.tsx"
      provides: "Reusable class card with Recharts visualization"
    - path: "src/components/ClassCard.test.tsx"
      provides: "Vitest unit tests for class rendering"
  key_links:
    - from: "src/pages/Results.tsx"
      to: "src/components/ClassCard.tsx"
      via: "props mapping"
---

<objective>
Redesign results presentation around names, summaries, and distribution visuals.
Add unit tests to ensure the mapping logic and UI components are reliable.

Purpose: Make optimization outcomes transparent and verifiable.
Output: Enhanced Results UI with names, charts, and unit tests.
</objective>

<execution_context>
@C:/Users/HP/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-trustworthy-optimization-results/03-CONTEXT.md
@.planning/phases/03-trustworthy-optimization-results/03-RESEARCH.md
@.planning/phases/03-trustworthy-optimization-results/03-VALIDATION.md
@src/pages/Results.tsx
@src/lib/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ClassCard Component with Recharts</name>
  <files>src/pages/Results.tsx, src/components/ClassCard.tsx</files>
  <action>
    Install `recharts` if not present (`npm install recharts`) and implement the `ClassCard`:
    - Create `src/components/ClassCard.tsx` to handle class-level rendering.
    - Fetch all project pupils in `Results.tsx` and map IDs to full `Pupil` objects.
    - Pass mapped pupil lists to `ClassCard`.
    - Implement Donut chart for Gender and Bar chart for Origin School using Recharts.
    - Ensure names are full and scrollable as per CONTEXT.md.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>ClassCard renders with real names and distribution charts.</done>
</task>

<task type="auto">
  <name>Task 2: Add Chemistry Link Icons to Results</name>
  <files>src/components/ClassCard.tsx</files>
  <action>
    Enhance `ClassCard` to show chemistry link indicators:
    - Receive `chemistry_links` as props.
    - For each pupil, check if their links (positive/negative) are satisfied within the same class.
    - Add visual icons (Green heart for positive link, Red circle for negative block violation).
    - Add tooltips describing the relationship.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Chemistry links are visually flagged in the pupil list of each class card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit Tests for ClassCard and Mapping Logic</name>
  <files>src/components/ClassCard.test.tsx</files>
  <behavior>
    - `ClassCard` must correctly count and display gender distribution for the chart.
    - Mapping logic must correctly resolve names from a provided pupil map.
    - It must handle empty classes or missing pupil data gracefully.
  </behavior>
  <action>
    Create `src/components/ClassCard.test.tsx` using Vitest:
    - Test that the component renders the correct number of pupil names.
    - Mock the pupil map and verify names are displayed correctly.
    - Assert that Recharts components (Pie/Bar) are rendered with expected data arrays.
    - Test the "satisfied chemistry" logic (icon visibility).
  </action>
  <verify>
    <automated>npx vitest run src/components/ClassCard.test.tsx</automated>
  </verify>
  <done>Unit tests verify the integrity of the results UI and data mapping.</done>
</task>

</tasks>

<verification>
- Run Vitest: `npx vitest run`.
- Confirm `ClassCard` correctly aggregates data for charts.
- Manual check: Open Results page, confirm charts and icons appear.
</verification>

<success_criteria>
- Pupil IDs are resolved to names in the UI.
- Recharts distributions are visible on every class card.
- Unit tests pass for the core mapping and rendering logic.
- Success criteria S3.2 from VALIDATION.md is satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-trustworthy-optimization-results/03-02-SUMMARY.md`
</output>
