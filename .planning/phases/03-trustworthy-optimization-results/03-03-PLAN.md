---
phase: 03-trustworthy-optimization-results
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [api/optimizer.py, src/pages/Results.tsx, api/test_fixtures.py]
autonomous: true
requirements: [OPTI-03]
must_haves:
  truths:
    - "Scores are presented as positive Satisfaction percentages"
    - "Results explicitly list sacrificed priorities to explain tradeoffs"
    - "Worst-performing classes for each category are visually highlighted"
  artifacts:
    - path: "api/optimizer.py"
      provides: "Tradeoff analysis and outlier detection"
    - path: "api/test_fixtures.py"
      provides: "Regression test suite with scenario-based assertions"
  key_links:
    - from: "src/pages/Results.tsx"
      to: "api/optimizer"
      via: "debug property of response"
---

<objective>
Enhance score explainability and establish solver stability via regression fixtures.
Make it clear why specific decisions were made by the solver and ensure behavior remains consistent over time.

Purpose: Foster trust and defensibility in the optimization outcomes.
Output: Explainability features and a stable solver regression suite.
</objective>

<execution_context>
@C:/Users/HP/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-trustworthy-optimization-results/03-CONTEXT.md
@.planning/phases/03-trustworthy-optimization-results/03-RESEARCH.md
@.planning/phases/03-trustworthy-optimization-results/03-VALIDATION.md
@api/optimizer.py
@src/pages/Results.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Tradeoff Detection and Outlier Analysis</name>
  <files>api/optimizer.py</files>
  <behavior>
    - Individual scores (Gender, Origin, etc.) must be normalized to 0-1.0.
    - If a priority (e.g., gender) has a significantly higher penalty than others, it must be flagged as "sacrificed".
    - Outlier class indices for each category must be provided in the debug response.
  </behavior>
  <action>
    Enhance the solver logic in `api/optimizer.py`:
    - Ensure all `Score` values are positive `float` percentages (0.0 to 1.0).
    - Analyze penalties to identify which soft constraints were traded off to satisfy others.
    - Add `sacrificed_priorities` and `worst_class_highlights` to the `debug` metadata in `OptimizeResponse`.
  </action>
  <verify>
    <automated>pytest api/test_scores_explainability.py</automated>
  </verify>
  <done>Solver provides tradeoff insights and identifies poor-performing classes in results metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard Explainability Features</name>
  <files>src/pages/Results.tsx</files>
  <action>
    Update `Results.tsx` to display explainability insights:
    - Render a "Tradeoffs and Sacrifices" section explaining which priorities were compromised (e.g., "Gender Balance was reduced to meet 100% of Chemistry Links").
    - Highlight class cards using the `worst_class_highlights` from the API.
    - Ensure global scores use the "Satisfaction percentage" terminology (already partially implemented).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Results dashboard provides human-readable context for tradeoffs and outliers.</done>
</task>

<task type="auto">
  <name>Task 3: Established Regression Fixtures Suite</name>
  <files>api/test_fixtures.py</files>
  <action>
    Create stable regression tests for the solver:
    - Define JSON fixtures for specific scenarios (e.g., `perfectly_balanced`, `chemistry_conflict`, `boundary_sizes`).
    - Implement `api/test_fixtures.py` that executes the solver against these inputs and asserts specific score/assignment invariants.
    - This suite ensures that future solver tweaks don't regress core behavior.
  </action>
  <verify>
    <automated>pytest api/test_fixtures.py</automated>
  </verify>
  <done>Solver stability is protected by a suite of gold-standard regression fixtures.</done>
</task>

</tasks>

<verification>
- Run regression tests: `pytest api/test_fixtures.py`.
- View results summary: Ensure tradeoffs and outlier highlights are visible.
- Verify 03-VALIDATION.md criteria S3.3 is addressed.
</verification>

<success_criteria>
- Solver behavior is verifiable via regression fixtures.
- UI explicitly highlights which priorities were traded off.
- Results scores are consistent, positive, and easy to explain.
</success_criteria>

<output>
After completion, create `.planning/phases/03-trustworthy-optimization-results/03-03-SUMMARY.md`
</output>
