---
phase: 03-trustworthy-optimization-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/optimizer.py, src/lib/api.ts, src/pages/PupilData.tsx]
autonomous: true
requirements: [OPTI-01]
must_haves:
  truths:
    - "API returns a structured DiagnosticResponse on 400 errors when optimization fails"
    - "Pupil Data screen displays specific violation messages and suggestions in a toast/summary"
  artifacts:
    - path: "api/optimizer.py"
      provides: "DiagnosticResponse model and infeasibility logic"
    - path: "src/lib/api.ts"
      provides: "Frontend types for DiagnosticResponse"
    - path: "src/pages/PupilData.tsx"
      provides: "Error handling for 400 status with diagnostic display"
  key_links:
    - from: "api/optimizer.py"
      to: "OR-Tools Solver"
      via: "SufficientAssumptionsForInfeasibility"
---

<objective>
Improve optimizer request validation and error modeling.
Define a clear `DiagnosticResponse` for 400 errors and update the frontend to show actionable feedback when optimization is mathematically impossible.

Purpose: Provide transparency when constraints cannot be met.
Output: Structured diagnostic 400 errors and a descriptive "Pupil Data Toast".
</objective>

<execution_context>
@C:/Users/HP/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-trustworthy-optimization-results/03-CONTEXT.md
@.planning/phases/03-trustworthy-optimization-results/03-RESEARCH.md
@.planning/phases/03-trustworthy-optimization-results/03-VALIDATION.md
@api/optimizer.py
@src/lib/api.ts
@src/pages/PupilData.tsx

<interfaces>
From api/optimizer.py:
```python
class OptimizeResponse(BaseModel):
    classes: List[OptimizedClass]
    score: Score
    debug: Dict[str, object] = {}
```

From src/lib/api.ts:
```typescript
export type OptimizeResponse = {
  classes: OptimizedClass[];
  score: Score;
  debug?: {
    chosenClassCount?: number;
    objective?: number;
  };
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define DiagnosticResponse Models</name>
  <files>api/optimizer.py, src/lib/api.ts</files>
  <action>
    Define a shared model for diagnostic errors:
    - In `api/optimizer.py`: Create `Violation(BaseModel)` with `category: str`, `message: str`, and `suggestion: str`. Create `DiagnosticResponse(BaseModel)` with `violations: List[Violation]`.
    - In `src/lib/api.ts`: Export matching TypeScript types `Violation` and `DiagnosticResponse`.
    - Ensure `DiagnosticResponse` is used as the detail in 400 `HTTPException` responses from the optimizer.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Structured diagnostic models are defined in both API and frontend.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Infeasibility Diagnosis in API</name>
  <files>api/optimizer.py</files>
  <behavior>
    - Hard constraints (Min/Max size, Strict Priorities) must be wrapped in Enforcement Literals.
    - If solver returns INFEASIBLE, it must return a DiagnosticResponse with a non-empty list of violations.
    - Test: Request with Min Size > Total Pupils must return "Min Class Size" violation.
  </behavior>
  <action>
    Update `_solve_for_k` and `_optimize_request` to identify failures:
    - Use `model.Add(...).OnlyEnforceIf(lit)` for hard constraints.
    - Call `solver.SufficientAssumptionsForInfeasibility()` on `INFEASIBLE`.
    - Map literals to `Violation` objects (e.g., `min_size_lit` -> "Decrease Min Class Size").
    - Return a 400 response with the `DiagnosticResponse` JSON.
  </action>
  <verify>
    <automated>pytest api/test_optimizer.py</automated>
  </verify>
  <done>API correctly diagnoses infeasibility and returns structured violations.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Pupil Data Toast for Diagnostic Errors</name>
  <files>src/pages/PupilData.tsx</files>
  <action>
    Update `runOptimizer` in `PupilData.tsx` to handle 400 diagnostic errors:
    - Catch the 400 error and parse the `DiagnosticResponse` from the body.
    - Display the specific violations as a descriptive toast or a dedicated error summary banner at the top of the table.
    - Use `toast.error` with a multi-line message or a custom toast component for better visibility.
    - Ensure the user remains on the Pupil Data screen to adjust parameters.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Frontend displays human-readable diagnostic feedback on optimization failure.</done>
</task>

</tasks>

<verification>
- Trigger an impossible optimization run (e.g., Min Size > Pupils).
- Confirm a detailed toast appears with the violation and relaxation suggestion.
- Verify 03-VALIDATION.md criteria S3.1 is addressed.
</verification>

<success_criteria>
- DiagnosticResponse model is implemented.
- OR-Tools infeasibility core is mapped to human-readable violations.
- PupilData screen displays actionable feedback on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-trustworthy-optimization-results/03-01-SUMMARY.md`
</output>
