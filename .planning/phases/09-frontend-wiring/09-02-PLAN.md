---
phase: 09-frontend-wiring
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified: []
autonomous: false
requirements:
  - WIRE-01
  - WIRE-02

must_haves:
  truths:
    - "The production Vercel deployment calls the Cloud Run URL for all optimization requests"
    - "A logged-in user can run optimization end-to-end without CORS or auth errors"
    - "The Supabase Bearer token is forwarded through Cloud Run to Supabase with RLS enforced"
    - "The optimizer request in the Network tab goes to https://optimizer-ek4bkd34ja-ew.a.run.app/project"
  artifacts: []
  key_links:
    - from: "Vercel dashboard env var VITE_OPTIMIZER_URL"
      to: "compiled bundle fetch URL"
      via: "Vite build-time injection"
      pattern: "optimizer-ek4bkd34ja-ew.a.run.app"
---

<objective>
Set the VITE_OPTIMIZER_URL environment variable in the Vercel dashboard and validate the full end-to-end optimization flow in the live production deployment.

Purpose: The code changes from Plan 01 are inert until the Vercel environment variable is configured. This plan gates the phase on a human-verified production smoke test per the locked decision in CONTEXT.md.

Output: Vercel env var configured, production redeployment triggered, end-to-end smoke test passed.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-frontend-wiring/09-CONTEXT.md
@.planning/phases/09-frontend-wiring/09-RESEARCH.md
@.planning/phases/09-frontend-wiring/09-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set VITE_OPTIMIZER_URL in Vercel dashboard</name>
  <read_first>
    - .planning/phases/09-frontend-wiring/09-CONTEXT.md (locked decision: set for ALL environments — Production and Preview)
  </read_first>
  <action>
The user must set the environment variable in the Vercel dashboard. Claude cannot access the Vercel dashboard UI.

**Steps for the user:**

1. Go to the Vercel Dashboard for the SkoleGeni project
2. Navigate to Settings -> Environment Variables
3. Add a new environment variable:
   - **Key:** `VITE_OPTIMIZER_URL`
   - **Value:** `https://optimizer-ek4bkd34ja-ew.a.run.app`
   - **Environments:** Check BOTH "Production" AND "Preview" (per locked decision)
4. Save the variable
5. Trigger a redeployment: go to the Deployments tab, find the latest production deployment, click the three-dot menu, select "Redeploy"
6. Wait for the redeployment to complete (typically 1-2 minutes)

**Why a redeploy is required:** Vite bakes `import.meta.env.VITE_*` values at build time. The current production bundle was compiled without this env var, so it falls back to `/api/optimizer`. A new build is needed for the Cloud Run URL to be compiled into the bundle.
  </action>
  <verify>After redeployment completes, the user confirms the deployment is live and the env var was saved for both Production and Preview.</verify>
  <acceptance_criteria>
    - VITE_OPTIMIZER_URL is set in Vercel dashboard for Production environment
    - VITE_OPTIMIZER_URL is set in Vercel dashboard for Preview environment
    - A new production deployment has completed after the env var was added
  </acceptance_criteria>
  <resume-signal>Type "done" when the env var is set and redeployment is complete</resume-signal>
  <done>VITE_OPTIMIZER_URL is configured in Vercel for both Production and Preview, and a fresh deployment is live.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end production smoke test</name>
  <read_first>
    - .planning/phases/09-frontend-wiring/09-CONTEXT.md (locked smoke test protocol)
    - .planning/phases/09-frontend-wiring/09-RESEARCH.md (pitfalls 1-4 and smoke test checklist)
  </read_first>
  <action>
Plan 01 added the 401 error handler in PupilData.tsx, created unit tests for env var reading and auth error flow, and updated .env.example. Task 1 of this plan set the VITE_OPTIMIZER_URL env var in Vercel and triggered a redeployment. Together these changes make the production frontend call the Cloud Run optimizer instead of localhost.

The user must now perform the production smoke test per the locked decision in CONTEXT.md to validate the full end-to-end flow.
  </action>
  <verify>
Per the locked decision in CONTEXT.md, perform this exact smoke test:

1. Open the Vercel production URL in Chrome (or any browser with DevTools)
2. Open DevTools (F12) -> Network tab (keep it open throughout)
3. Sign in with a real Supabase user account
4. Open an existing project that has pupils
5. Click "Run Optimizer"
6. Wait for the result to appear

**Check these specific things:**

A. **Result appears:** The optimization completes and navigates to the results page — not an error toast.

B. **Network tab — correct URL:** Find the POST request to `/project`. The Request URL MUST start with `https://optimizer-ek4bkd34ja-ew.a.run.app/project` (NOT localhost, NOT `/api/optimizer`).

C. **Network tab — auth header:** Click the optimizer POST request, look at Request Headers. There MUST be an `Authorization: Bearer <token>` header present.

D. **Network tab — 200 response:** The optimizer POST response status MUST be 200.

E. **Console tab — zero errors:** Switch to the Console tab. There MUST be zero CORS errors and zero auth errors. (Ignore unrelated React DevTools warnings if any.)

If ANY of checks A-E fail, describe the failure. Common issues:
- If URL is `/api/optimizer` instead of Cloud Run: env var not set or redeploy not triggered
- If CORS error in console: Cloud Run service may not be `--allow-unauthenticated`
- If 401 response: token may be expired — sign out, sign back in, retry
- If 404 or 502: Cloud Run service may be down — check `curl https://optimizer-ek4bkd34ja-ew.a.run.app/health`
  </verify>
  <acceptance_criteria>
    - Optimization POST request URL starts with https://optimizer-ek4bkd34ja-ew.a.run.app
    - Authorization: Bearer header is present on the request
    - Response status is 200
    - Browser console shows zero CORS errors
    - Result page renders with optimization output
  </acceptance_criteria>
  <resume-signal>Type "approved" if all checks pass, or describe the specific failure</resume-signal>
  <done>A logged-in user on the production Vercel URL can run an optimization that hits Cloud Run, receives a result, and has zero CORS or auth errors in the browser console.</done>
</task>

</tasks>

<verification>
All four Phase 9 success criteria validated:
1. Production Vercel calls Cloud Run URL (not localhost) — verified via Network tab
2. Logged-in user runs optimization without CORS or auth errors — verified via Console tab
3. Supabase Bearer token forwarded through Cloud Run — verified via Network tab Authorization header + successful RLS-gated data read
4. 401 handling implemented — verified by unit test in Plan 01 (live 401 testing is destructive and not required)
</verification>

<success_criteria>
- Vercel env var VITE_OPTIMIZER_URL is set for Production and Preview
- Production smoke test passes all five checks (A-E)
- The phase is complete: frontend calls Cloud Run, auth flows end-to-end, 401 is handled
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-wiring/09-02-SUMMARY.md`
</output>
