---
phase: 05-polish-and-release-readiness
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/components/layout/AppShell.tsx
  - src/components/pupil/ChemistryStatCards.tsx
  - src/pages/PupilData.tsx
autonomous: true
gap_closure: true
requirements: [UX-01]

must_haves:
  truths:
    - "Project-scoped screens feel materially closer to the dense admin reference, with stronger shell hierarchy and clearer active navigation emphasis."
    - "Pupil Data presents a more deliberate command surface with stronger title/action framing, denser table styling, and stat cards that read as polished dashboard summaries instead of MVP placeholders."
    - "The refreshed shell and Pupil Data layout preserve all existing workflow behavior while improving visual coherence during human browser review."
  artifacts:
    - path: "src/components/layout/AppShell.tsx"
      provides: "Heavier sidebar shell with top utility chrome and stronger active-state treatment"
      exports: ["AppShell"]
    - path: "src/pages/PupilData.tsx"
      provides: "Denser admin-style page framing and polished roster table/action layout"
      contains: "Quick Search"
    - path: "src/components/pupil/ChemistryStatCards.tsx"
      provides: "Higher-fidelity chemistry summary cards aligned with the tightened page chrome"
      exports: ["ChemistryStatCards"]
  key_links:
    - from: "src/pages/PupilData.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "shared shell header and page-level spacing hierarchy"
      pattern: "Quick Search|Roster Optimizer"
    - from: "src/pages/PupilData.tsx"
      to: "src/components/pupil/ChemistryStatCards.tsx"
      via: "dashboard-style summary cards rendered below the pupil table"
      pattern: "ChemistryStatCards"
---

<objective>
Close the Phase 5 screenshot-driven UX gap by making the shared shell and Pupil Data surface feel materially closer to the design reference.

Purpose: Resolve the human-review failure where the localhost UI is structurally correct but still looks too flat, sparse, and MVP-like compared to the denser admin reference.
Output: A heavier AppShell/sidebar plus a more polished Pupil Data page/table/stat-card composition that preserves existing behavior while improving perceived product quality.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release-readiness/05-CONTEXT.md
@.planning/phases/05-polish-and-release-readiness/05-RESEARCH.md
@.planning/phases/05-polish-and-release-readiness/05-UI-SPEC.md
@.planning/phases/05-polish-and-release-readiness/05-VERIFICATION.md
@.planning/phases/05-polish-and-release-readiness/05-02-SUMMARY.md
@src/components/layout/AppShell.tsx
@src/pages/PupilData.tsx
@src/components/pupil/ChemistryStatCards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen the shared shell to match the denser admin reference</name>
  <files>src/components/layout/AppShell.tsx</files>
  <read_first>
    - src/components/layout/AppShell.tsx
    - .planning/phases/05-polish-and-release-readiness/05-UI-SPEC.md
    - .planning/phases/05-polish-and-release-readiness/05-VERIFICATION.md
  </read_first>
  <action>Rework `src/components/layout/AppShell.tsx` so the project-scoped shell reads as a more substantial admin surface instead of a thin left rail. Keep the existing route structure and four links, but make the sidebar visually heavier with stronger branding hierarchy, larger vertical rhythm, and a more obvious active-state treatment than the current thin accent border. Add a top utility bar in the main content area that includes the literal text `Quick Search` plus two compact utility icon buttons on the right so the shell composition better matches the reference screenshot's header framing. Keep Welcome outside the shell and do not add animation, dark mode, or collapsible behavior. Use the existing SkoleGeni identity rather than copying the reference product name; the goal is closer composition and polish, not a brand swap.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/AppShell.tsx` contains the literal text `Quick Search`
    - `src/components/layout/AppShell.tsx` still contains the four nav labels `Configuration`, `Pupil Data`, `Results`, and `Class Editor`
    - `src/components/layout/AppShell.tsx` contains `SkoleGeni` and `Roster Optimizer`
    - `src/components/layout/AppShell.tsx` contains a top-level sidebar width class stronger than the current rail, such as `w-64` or an equivalent explicit 256px width
    - `src/components/layout/AppShell.tsx` contains a more substantial active-state treatment than only `border-l-2`, such as a filled background and stronger text/icon emphasis
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The shell now has stronger sidebar presence and top utility framing, while preserving the same project-scoped navigation behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Densify and polish the Pupil Data surface to match the reference composition</name>
  <files>src/pages/PupilData.tsx, src/components/pupil/ChemistryStatCards.tsx</files>
  <read_first>
    - src/pages/PupilData.tsx
    - src/components/pupil/ChemistryStatCards.tsx
    - .planning/phases/05-polish-and-release-readiness/05-UI-SPEC.md
    - .planning/phases/05-polish-and-release-readiness/05-02-SUMMARY.md
  </read_first>
  <action>Restyle the Pupil Data page so it feels closer to the dense admin reference and less like a basic form page. Keep all current behavior, save flow, chemistry logic, import flow, and optimizer actions intact, but tighten the visual hierarchy around the title, action row, pupil table, and chemistry stat cards. Specifically: give the page a more deliberate section frame with clearer header/action separation; make the table read more like a polished management grid with stronger row/header distinction; make the chemistry buttons and counts feel intentional rather than bare utility controls; and upgrade `ChemistryStatCards` so the cards read as dashboard summaries with stronger value hierarchy, icon framing, and cleaner spacing. Do not change the underlying workflow text unless necessary for layout clarity, and do not remove any existing controls or diagnostics.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/PupilData.tsx` still contains the texts `Pupil Mode`, `Import CSV`, `Add Row`, and `Run Optimizer`
    - `src/pages/PupilData.tsx` still renders `WorkflowStatusHeader`, `IssuesPanel`, and `ChemistryStatCards`
    - `src/components/pupil/ChemistryStatCards.tsx` still contains the labels `Pupils with chemistry links`, `Positive pairs`, and `Negative pairs`
    - `src/components/pupil/ChemistryStatCards.tsx` contains stronger card framing than the current minimal cards, including explicit border/background classes and a separate visual treatment for the metric value area
    - `src/pages/PupilData.tsx` contains a clearer table/header framing than the current single bordered box, using explicit section or sub-surface wrappers rather than one flat container only
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Pupil Data keeps its behavior but now presents a denser, more polished shell/header/table/card composition that reads closer to the reference screenshot.</done>
</task>

</tasks>

<verification>
Run `npm run build`, then compare the refreshed Pupil Data route in the browser against the screenshot reference focusing on sidebar weight, top utility framing, and denser table/stat-card polish.
</verification>

<success_criteria>
The shared shell and Pupil Data page move materially closer to the screenshot reference in density, hierarchy, and polish without changing workflow behavior, and a follow-up browser review can confirm the visual gap is substantially reduced.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-release-readiness/05-04-SUMMARY.md`
</output>
