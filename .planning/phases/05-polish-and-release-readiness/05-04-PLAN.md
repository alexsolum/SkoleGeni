---
phase: 05-polish-and-release-readiness
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/components/layout/AppShell.tsx
  - src/components/pupil/ChemistryStatCards.tsx
  - src/pages/PupilData.tsx
autonomous: false
gap_closure: true
requirements: [UX-01]

must_haves:
  truths:
    - "Project-scoped screens keep the existing four-link sidebar, SkoleGeni branding, and route behavior without adding new shell controls or routes"
    - "Pupil Data keeps its current workflow controls while adding denser section framing and more explicit table/stat-card presentation"
    - "Final judgment about whether the refreshed shell and Pupil Data polish close the screenshot-derived UX gap is deferred to the human review gate"
  artifacts:
    - path: "src/components/layout/AppShell.tsx"
      provides: "Heavier sidebar shell with stronger branding and active-state treatment, without new utility chrome"
      exports: ["AppShell"]
    - path: "src/pages/PupilData.tsx"
      provides: "Denser admin-style page framing and polished roster table/action layout"
      contains: "ChemistryStatCards"
    - path: "src/components/pupil/ChemistryStatCards.tsx"
      provides: "Higher-fidelity chemistry summary cards aligned with the tightened page chrome"
      exports: ["ChemistryStatCards"]
  key_links:
    - from: "src/pages/PupilData.tsx"
      to: "src/components/layout/AppShell.tsx"
      via: "shared sidebar shell and page-level spacing hierarchy"
      pattern: "ChemistryStatCards|Run Optimizer"
    - from: "src/pages/PupilData.tsx"
      to: "src/components/pupil/ChemistryStatCards.tsx"
      via: "dashboard-style summary cards rendered below the pupil table"
      pattern: "ChemistryStatCards"
---

<objective>
Close the Phase 5 screenshot-driven UX gap by making the shared shell and Pupil Data surface feel materially closer to the design reference.

Purpose: Resolve the human-review failure where the localhost UI is structurally correct but still looks too flat, sparse, and MVP-like compared to the denser admin reference.
Output: A heavier AppShell/sidebar plus a more polished Pupil Data page/table/stat-card composition that preserves existing behavior while improving perceived product quality.
</objective>

<execution_context>
@C:/Users/HP/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release-readiness/05-CONTEXT.md
@.planning/phases/05-polish-and-release-readiness/05-RESEARCH.md
@.planning/phases/05-polish-and-release-readiness/05-UI-SPEC.md
@.planning/phases/05-polish-and-release-readiness/05-VERIFICATION.md
@.planning/phases/05-polish-and-release-readiness/05-02-SUMMARY.md
@src/components/layout/AppShell.tsx
@src/pages/PupilData.tsx
@src/components/pupil/ChemistryStatCards.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen the shared shell to match the denser admin reference</name>
  <files>src/components/layout/AppShell.tsx</files>
  <read_first>
    - src/components/layout/AppShell.tsx
    - .planning/phases/05-polish-and-release-readiness/05-UI-SPEC.md
    - .planning/phases/05-polish-and-release-readiness/05-VERIFICATION.md
  </read_first>
  <action>Rework `src/components/layout/AppShell.tsx` so the project-scoped shell reads as a more substantial admin surface instead of a thin left rail. Keep the existing route structure and four links, but make the sidebar visually heavier with stronger branding hierarchy, larger vertical rhythm, and a more obvious active-state treatment than the current thin accent border. Stay within the 05-UI-SPEC contract: no top utility bar, no `Quick Search`, no utility buttons, no new routes, and no new shell controls. Keep Welcome outside the shell and do not add animation, dark mode, or collapsible behavior. Use the existing SkoleGeni identity rather than copying the reference product name; the goal is closer composition and polish inside the approved shell structure.</action>
  <verify>
    <automated>cd "C:/Users/HP/Documents/Koding/SkoleGeni" && node -e "const fs=require('fs'); const shell=fs.readFileSync('src/components/layout/AppShell.tsx','utf8'); const required=['Configuration','Pupil Data','Results','Class Editor','SkoleGeni','Roster Optimizer']; const missing=required.filter((token)=>!shell.includes(token)); if(missing.length){console.error('Missing AppShell tokens:', missing.join(', ')); process.exit(1);} const banned=['Quick Search','Search','utility']; const bannedFound=banned.filter((token)=>shell.includes(token)); if(bannedFound.length){console.error('Unsanctioned shell tokens present:', bannedFound.join(', ')); process.exit(1);} if(!/(w-64|w-\\[256px\\]|max-w-\\[256px\\])/.test(shell)){console.error('Expected heavier sidebar width token in AppShell'); process.exit(1);} if(!(shell.includes('bg-accent/10') && /font-(medium|semibold|bold)/.test(shell))){console.error('Expected stronger active-state treatment in AppShell'); process.exit(1);} console.log('AppShell shell-polish checks passed');" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/AppShell.tsx` still contains the four nav labels `Configuration`, `Pupil Data`, `Results`, and `Class Editor`
    - `src/components/layout/AppShell.tsx` contains `SkoleGeni` and `Roster Optimizer`
    - `src/components/layout/AppShell.tsx` contains a top-level sidebar width class stronger than the current rail, such as `w-64` or an equivalent explicit 256px width
    - `src/components/layout/AppShell.tsx` contains a more substantial active-state treatment than only `border-l-2`, such as a filled background and stronger text/icon emphasis
    - `src/components/layout/AppShell.tsx` does NOT introduce `Quick Search`, utility buttons, or new shell actions outside the approved four-link navigation
    - The automated AppShell check fails if required nav labels, branding, stronger width token, or stronger active-state treatment is missing, or if unsanctioned shell controls appear
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The shell now has stronger sidebar presence while preserving the same approved project-scoped navigation behavior and without adding unauthorized shell controls.</done>
</task>

<task type="auto">
  <name>Task 2: Densify and polish the Pupil Data surface to match the reference composition</name>
  <files>src/pages/PupilData.tsx, src/components/pupil/ChemistryStatCards.tsx</files>
  <read_first>
    - src/pages/PupilData.tsx
    - src/components/pupil/ChemistryStatCards.tsx
    - .planning/phases/05-polish-and-release-readiness/05-UI-SPEC.md
    - .planning/phases/05-polish-and-release-readiness/05-02-SUMMARY.md
  </read_first>
  <action>Restyle the Pupil Data page so it feels closer to the dense admin reference and less like a basic form page. Keep all current behavior, save flow, chemistry logic, import flow, and optimizer actions intact, but tighten the visual hierarchy around the title, action row, pupil table, and chemistry stat cards. Specifically: give the page a more deliberate section frame with clearer header/action separation; make the table read more like a polished management grid with stronger row/header distinction; make the existing chemistry controls and counts feel intentional rather than bare utility controls; and upgrade `ChemistryStatCards` so the cards read as dashboard summaries with stronger value hierarchy, icon framing, and cleaner spacing. Do not add new commands, search fields, or utility-button actions, do not change the underlying workflow text unless necessary for layout clarity, and do not remove any existing controls or diagnostics.</action>
  <verify>
    <automated>cd "C:/Users/HP/Documents/Koding/SkoleGeni" && node -e "const fs=require('fs'); const page=fs.readFileSync('src/pages/PupilData.tsx','utf8'); const cards=fs.readFileSync('src/components/pupil/ChemistryStatCards.tsx','utf8'); const pageTokens=['Pupil Mode','Import CSV','Add Row','Run Optimizer','WorkflowStatusHeader','IssuesPanel','ChemistryStatCards']; const cardTokens=['Pupils with chemistry links','Positive pairs','Negative pairs']; const missingPage=pageTokens.filter((token)=>!page.includes(token)); const missingCards=cardTokens.filter((token)=>!cards.includes(token)); if(missingPage.length){console.error('Missing PupilData tokens:', missingPage.join(', ')); process.exit(1);} if(missingCards.length){console.error('Missing ChemistryStatCards labels:', missingCards.join(', ')); process.exit(1);} if(!/(section|header|rounded-|border-|bg-)/.test(page)){console.error('Expected stronger section framing in PupilData'); process.exit(1);} if(!(/border-/.test(cards) && /bg-/.test(cards) && /(rounded-|p-)/.test(cards))){console.error('Expected stronger card framing in ChemistryStatCards'); process.exit(1);} console.log('Pupil Data polish checks passed');" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/PupilData.tsx` still contains the texts `Pupil Mode`, `Import CSV`, `Add Row`, and `Run Optimizer`
    - `src/pages/PupilData.tsx` still renders `WorkflowStatusHeader`, `IssuesPanel`, and `ChemistryStatCards`
    - `src/components/pupil/ChemistryStatCards.tsx` still contains the labels `Pupils with chemistry links`, `Positive pairs`, and `Negative pairs`
    - `src/components/pupil/ChemistryStatCards.tsx` contains stronger card framing than the current minimal cards, including explicit border/background classes and a separate visual treatment for the metric value area
    - `src/pages/PupilData.tsx` contains a clearer table/header framing than the current single bordered box, using explicit section or sub-surface wrappers rather than one flat container only
    - `src/pages/PupilData.tsx` does NOT introduce new search inputs, new utility buttons, or new workflow actions beyond the existing page controls
    - The automated Pupil Data check fails if required workflow labels/components disappear or if the page/cards do not add explicit section and card framing classes
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Pupil Data keeps its behavior but now presents a denser, more polished shell/header/table/card composition that reads closer to the reference screenshot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human review gate for the screenshot-derived UX gap</name>
  <action>Run the browser review after the automated shell and Pupil Data polish work completes. This checkpoint decides whether the narrowed UX gap is actually closed; it does not authorize additional features beyond the approved scope.</action>
  <what-built>Shell and Pupil Data visual refinements constrained to the screenshot-derived UX gap, without new shell controls or workflow actions.</what-built>
  <how-to-verify>
1. Open Welcome (`/`) and confirm it remains sidebar-free.
2. Open Configuration, Pupil Data, Results, and Class Editor for the same project and confirm the persistent sidebar remains present on every project-scoped route.
3. Click through the sidebar links and confirm the active navigation state remains clearly blue-highlighted during route changes.
4. On Pupil Data, confirm the page now reads denser and more deliberate through spacing, framing, table hierarchy, and stat-card presentation, while preserving the existing actions `Import CSV`, `Add Row`, and `Run Optimizer`.
5. Confirm no new search bar, utility buttons, or extra shell actions were added.
6. Decide whether the refreshed shell and Pupil Data polish are sufficient to close the screenshot-derived UX gap for `UX-01`.
  </how-to-verify>
  <verify>Human review gate must be completed using the numbered browser checks below.</verify>
  <done>The user explicitly confirms the narrowed UX gap is closed, or returns concrete visual issues for another gap-closure revision.</done>
  <resume-signal>Type "approved" if the UX gap is closed, or describe the remaining visual issues.</resume-signal>
</task>

</tasks>

<verification>
Run the two automated checks above plus `npm run build`, then complete the blocking human review gate below before marking the plan done.

1. Open Welcome (`/`) and confirm it remains sidebar-free.
2. Open Configuration, Pupil Data, Results, and Class Editor for the same project and confirm the persistent sidebar remains present on every project-scoped route.
3. Click through the sidebar links and confirm the active navigation state remains clearly blue-highlighted during route changes.
4. Compare Welcome, Configuration, Pupil Data, Results, and Class Editor against each other and confirm typography, spacing, color usage, cards, and shared chrome read as one desktop-first SkoleGeni product.
5. On Pupil Data specifically, compare the refreshed shell weight, table density, and chemistry stat-card polish against the screenshot-derived gap without requiring new features beyond this gap closure.
</verification>

<success_criteria>
The shared shell and Pupil Data page add concrete framing and hierarchy improvements without changing workflow behavior or adding unauthorized shell controls, the automated checks prove the approved code changes exist, and the blocking human review confirms the screenshot-derived `UX-01` gap is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-release-readiness/05-04-SUMMARY.md`
</output>
