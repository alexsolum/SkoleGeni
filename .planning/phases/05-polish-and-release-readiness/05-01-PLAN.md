---
phase: 05-polish-and-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/index.css
  - tailwind.config.js
  - index.html
  - eslint.config.js
  - .prettierrc
  - .gitignore
autonomous: true
requirements: [QUAL-02]

must_haves:
  truths:
    - "npm run build succeeds with zero errors on the updated toolchain"
    - "npm run lint runs ESLint with TypeScript and React rules"
    - "npx prettier --check src/ reports formatting status"
    - "All package.json dependency versions are pinned to exact resolved versions (no * wildcards)"
    - "Body text renders in Satoshi font, not Manrope"
    - ".gitignore prevents node_modules, dist, __pycache__, .env from being tracked"
  artifacts:
    - path: "package.json"
      provides: "Pinned dependency versions"
      contains: "\"react\": \"19.2.4\""
    - path: "eslint.config.js"
      provides: "ESLint v9 flat config with TS and React rules"
      contains: "tseslint"
    - path: ".gitignore"
      provides: "Standard project ignores"
      contains: "node_modules"
    - path: "src/index.css"
      provides: "Satoshi font import replacing Manrope"
      contains: "Satoshi"
    - path: "tailwind.config.js"
      provides: "Updated font family config"
      contains: "Satoshi"
  key_links:
    - from: "src/index.css"
      to: "tailwind.config.js"
      via: "@config directive"
      pattern: "@config.*tailwind.config.js"
    - from: "eslint.config.js"
      to: "typescript"
      via: "typescript-eslint parser"
      pattern: "tseslint"
---

<objective>
Upgrade and document the frontend toolchain and design-token implementation.

Purpose: Resolve QUAL-02 so the project runs on a current, documented toolchain without outdated conventions. Pin all wildcard dependencies, add ESLint/Prettier, replace Manrope with Satoshi, add .gitignore.
Output: Clean toolchain with linting, formatting, pinned versions, and correct fonts.
</objective>

<execution_context>
@C:/Users/HP/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-and-release-readiness/05-CONTEXT.md
@.planning/phases/05-polish-and-release-readiness/05-RESEARCH.md
@.planning/phases/05-polish-and-release-readiness/05-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin dependency versions, add .gitignore, and install ESLint/Prettier</name>
  <files>package.json, .gitignore</files>
  <read_first>
    - package.json (current wildcard versions)
    - package-lock.json (resolved versions to pin)
  </read_first>
  <action>
1. Edit `package.json` to replace every `"*"` version with the exact resolved version from `package-lock.json`. The complete mapping is:

**dependencies:**
- `"@dnd-kit/core": "6.3.1"`
- `"@dnd-kit/sortable": "10.0.0"`
- `"@dnd-kit/utilities": "3.2.2"`
- `"@supabase/supabase-js": "2.99.2"`
- `"clsx": "2.1.1"`
- `"lodash.debounce": "4.0.8"`
- `"papaparse": "5.5.3"`
- `"react": "19.2.4"`
- `"react-dom": "19.2.4"`
- `"react-hot-toast": "2.6.0"`
- `"react-router-dom": "7.13.1"`
- `"tailwind-merge": "3.5.0"`
- Keep `"recharts": "^3.8.0"`, `"zundo": "^2.3.0"`, `"zustand": "^5.0.12"` as-is (already pinned with caret)

**devDependencies:**
- `"@playwright/test": "1.58.2"`
- `"@testing-library/jest-dom": "6.9.1"`
- `"@testing-library/react": "16.3.2"`
- `"@testing-library/user-event": "14.6.1"`
- `"@types/node": "25.5.0"`
- `"@types/react": "19.2.14"`
- `"@types/react-dom": "19.2.3"`
- `"@vitejs/plugin-react": "6.0.1"`
- `"autoprefixer": "10.4.27"`
- `"jsdom": "29.0.1"`
- `"postcss": "8.5.8"`
- `"tailwindcss": "4.2.2"`
- `"typescript": "5.9.3"`
- `"vite": "8.0.0"`
- `"vitest": "4.1.0"`
- Keep `"@tailwindcss/postcss": "^4.2.2"`, `"@types/papaparse": "^5.5.2"` as-is (already pinned with caret)

2. Add `"lint"` and `"format"` scripts to `package.json`:
```json
"lint": "eslint src/",
"format": "prettier --write src/",
"format:check": "prettier --check src/"
```

3. Run `npm install -D eslint @eslint/js @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint-plugin-react eslint-plugin-react-hooks eslint-config-prettier prettier typescript-eslint`

4. Create `.gitignore` with:
```
node_modules/
dist/
.env
.env.local
.env.*.local
__pycache__/
*.pyc
.DS_Store
test-results/
playwright-report/
tsconfig.tsbuildinfo
```

Do NOT run `npm install` just for pinning — only run it to add the new ESLint/Prettier dev dependencies.
  </action>
  <verify>
    <automated>cd "C:/Users/HP/Documents/Koding/SkoleGeni" && node -e "const p=require('./package.json'); const stars=Object.entries({...p.dependencies,...p.devDependencies}).filter(([k,v])=>v==='*'); if(stars.length>0){console.error('Wildcard versions remain:',stars);process.exit(1)}else{console.log('All versions pinned')}" && test -f .gitignore && echo ".gitignore exists"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"react": "19.2.4"` (not `"*"`)
    - package.json contains `"typescript": "5.9.3"` (not `"*"`)
    - package.json contains `"lint": "eslint src/"` script
    - package.json contains `"format": "prettier --write src/"` script
    - .gitignore contains `node_modules/`
    - .gitignore contains `__pycache__/`
    - .gitignore contains `.env`
    - No `"*"` version string remains in package.json dependencies or devDependencies
  </acceptance_criteria>
  <done>All package.json versions pinned to exact resolved versions, ESLint/Prettier installed, .gitignore created</done>
</task>

<task type="auto">
  <name>Task 2: Create ESLint flat config and replace Manrope with Satoshi font</name>
  <files>eslint.config.js, .prettierrc, src/index.css, tailwind.config.js, index.html</files>
  <read_first>
    - src/index.css (current Manrope import and body font-family)
    - tailwind.config.js (current fontFamily.body config)
    - index.html (check for existing font links)
  </read_first>
  <action>
1. Create `eslint.config.js` with ESLint v9 flat config:
```js
import eslint from "@eslint/js";
import tseslint from "typescript-eslint";
import reactPlugin from "eslint-plugin-react";
import reactHooksPlugin from "eslint-plugin-react-hooks";
import prettierConfig from "eslint-config-prettier";

export default tseslint.config(
  eslint.configs.recommended,
  ...tseslint.configs.recommended,
  {
    plugins: { react: reactPlugin, "react-hooks": reactHooksPlugin },
    rules: {
      "react/react-in-jsx-scope": "off",
      "react-hooks/rules-of-hooks": "error",
      "react-hooks/exhaustive-deps": "warn",
      "@typescript-eslint/no-unused-vars": ["warn", { argsIgnorePattern: "^_" }],
    },
    settings: {
      react: { version: "detect" },
    },
  },
  prettierConfig,
  {
    ignores: ["dist/", "node_modules/", "api/", "docker/", "stitch_skolegeni/"],
  }
);
```

2. Create `.prettierrc`:
```json
{
  "semi": true,
  "singleQuote": false,
  "trailingComma": "none",
  "printWidth": 100,
  "tabWidth": 2
}
```

3. Update `src/index.css`:
- Replace the Google Fonts import URL. Change `Manrope:wght@400;500;600;700` to load Satoshi from Fontshare instead.
- The first line should become:
```css
@import url("https://fonts.googleapis.com/css2?family=Cabinet+Grotesk:wght@400;500;700&family=JetBrains+Mono:wght@400;600&display=swap");
@import url("https://api.fontshare.com/v2/css?f[]=satoshi@400,500,700&display=swap");
```
  (Split into two imports since Cabinet Grotesk stays on Google Fonts and Satoshi is on Fontshare.)
- Change `body { font-family }` from `"Manrope"` to `"Satoshi"`:
```css
body {
  background: var(--color-background);
  color: var(--color-text);
  font-family: "Satoshi", ui-sans-serif, system-ui;
}
```

4. Update `tailwind.config.js`:
- Change `fontFamily.body` from `["Manrope", "ui-sans-serif", "system-ui"]` to `["Satoshi", "ui-sans-serif", "system-ui"]`
- Keep everything else unchanged (colors, borderRadius, heading/mono fonts, content paths)

5. Do NOT run `npm run lint` to fix existing code in this task. The lint setup is for ongoing use. Existing code may have warnings; that is acceptable.
  </action>
  <verify>
    <automated>cd "C:/Users/HP/Documents/Koding/SkoleGeni" && test -f eslint.config.js && echo "eslint.config.js exists" && grep -q "Satoshi" src/index.css && echo "Satoshi in index.css" && grep -q "Satoshi" tailwind.config.js && echo "Satoshi in tailwind.config.js" && grep -qv "Manrope" tailwind.config.js && echo "Manrope removed from tailwind.config.js" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.js contains `tseslint` import
    - eslint.config.js contains `eslint-config-prettier` (prettierConfig)
    - eslint.config.js contains `"react-hooks/exhaustive-deps": "warn"`
    - .prettierrc exists and contains `"semi": true`
    - src/index.css contains `fontshare.com` in an import URL
    - src/index.css contains `font-family: "Satoshi"` on the body rule
    - src/index.css does NOT contain the string `Manrope`
    - tailwind.config.js body font array starts with `"Satoshi"`
    - tailwind.config.js does NOT contain `Manrope`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>ESLint v9 flat config created, Prettier configured, Manrope fully replaced by Satoshi in both CSS and Tailwind config, build passes</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (no build errors with updated toolchain)
- `node -e "..."` confirms no wildcard versions remain
- `grep -q "Satoshi" src/index.css tailwind.config.js` confirms font replacement
- `.gitignore` exists with standard ignores
- `eslint.config.js` exists with TypeScript and React rules
</verification>

<success_criteria>
- All `*` versions in package.json replaced with exact pinned versions
- ESLint v9 flat config with TypeScript, React, React Hooks, and Prettier integration
- Satoshi replaces Manrope in src/index.css body rule and tailwind.config.js font family
- .gitignore prevents tracking of node_modules, dist, __pycache__, .env
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-release-readiness/05-01-SUMMARY.md`
</output>
